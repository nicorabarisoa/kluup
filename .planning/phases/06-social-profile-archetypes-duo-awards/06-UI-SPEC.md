---
phase: 06
slug: social-profile-archetypes-duo-awards
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-14
---

# Phase 6 — UI Design Contract
## Social Profile & Archetypes + Duo Awards

> Visual and interaction contract for Phase 6 frontend work. Generated by gsd-ui-researcher.
> All locked decisions sourced from 06-CONTEXT.md (D-01..D-08) and codebase scan.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | No `components.json` — manual Tailwind v4 tokens |
| Preset | not applicable | — |
| Component library | none (custom React components) | Codebase scan |
| Icon library | Unicode emoji only (existing pattern) | `lib/i18n.ts` — award emojis 🧲🧠⚔️🔥 |
| Font — display | Bricolage Grotesque **800**, var `--font-display` | `app/layout.tsx` |
| Font — body | DM Sans **400**, var `--font-body` | `app/layout.tsx` |

**shadcn gate result:** Not initialized. Project uses hand-rolled Tailwind v4 design tokens declared in `app/globals.css` and the `C` object in `game/page.tsx`. Registry safety gate: not applicable.

---

## Spacing Scale

Multiples of 4 only. All card layout uses explicit px inline styles (modern-screenshot discipline).

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, color dot size (8px = 2×xs) |
| sm | 8px | Pill gaps, trait bar bottom margin, award row gap |
| md | 16px | Card internal column gap (matches existing ShareCard `gap: 16`) |
| lg | 24px | Card section top padding, flip affordance bottom margin |
| xl | 32px | Card horizontal padding (matches existing `padding: '28px 32px'`) |
| 2xl | 48px | End-screen section separation |
| 3xl | 64px | Not used in this phase |

**Phase-specific exceptions:**

- Card capture container: **always explicit px** — no `%`-based widths inside the 540×540 `div`. Computed as `style={{ width: Npx }}`.
- Touch target for tap-to-flip: minimum **44×44px** hit area on the card wrapper (iOS HIG minimum).
- Top color bar on card: **6px** height (matches existing ShareCard pattern exactly).
- Trait bar track height: **8px**. Max bar width: **160px** (computed as `Math.round(pct / 100 * 160)` px for capture).
- Award emoji container: **32×32px** fixed (consistent alignment regardless of emoji width).

---

## Typography

### In-app type scale (live-DOM chrome) — 4 sizes

The Phase-A live-DOM chrome (flip affordance, Share button, end-screen text) uses this 4-size scale with **exactly 2 font weights** — Bricolage Grotesque 800 (display) and DM Sans 400 (everything else). **Emphasis comes from size, color, and casing — never extra weights.**

| Role | Size | Weight | Font | Line Height | Usage |
|------|------|--------|------|-------------|-------|
| Display — group title | 42px | 800 | Bricolage Grotesque | 1.1 | Face 1 hero (reuses existing ShareCard `h1`) |
| Display — archetype name | 28px | 800 | Bricolage Grotesque | 1.1 | Face 2 hero (UPPERCASE) |
| Body | 16px | 400 | DM Sans | 1.4 | Award names, player names, trait labels (emphasis via size + color, e.g. 16px `C.text` vs 13px `C.muted`) |
| Small / caption | 13px | 400 | DM Sans | 1.3 | Section labels (ALLCAPS + letter-spacing), trait %, byline, flip affordance, footer |

### Card inline-style zone (modern-screenshot discipline)

The entire card — the visible card **and** the off-screen capture `<div>` — is rendered with explicit inline `style={}` px values (no Tailwind classes), mirroring the existing `ShareCard` component. Its internal font-sizes mirror the 4 tiers above (display 42 / 28, body 16, small 13); any legacy value carried over from the current card is consolidated toward these tiers. Award emoji glyphs (🧲🧠⚔️🔥) render at an 18px glyph size and are **decorative, not part of the text type scale**. This inline zone is implementation that mirrors the live-DOM scale — it does not introduce new type tiers.

---

## Color

### Existing global tokens (do not change)

| Token | Hex | Tailwind / CSS var | Usage |
|-------|-----|--------------------|-------|
| `C.bg` | `#0D0D0D` | `--color-kluup-bg` | Card background (60% dominant surface) |
| `C.surface` | `#1A1A1A` | `--color-kluup-surface` | Inner blocks, trait bar track, award row bg |
| `C.border` | `#252525` | `--color-kluup-border` | Player pill border, section dividers |
| `C.text` | `#FFFFFF` | `--color-kluup-text` | Primary text, archetype name, award player names |
| `C.muted` | `#888888` | `--color-kluup-muted` | Section labels, trait % values |
| `C.faint` | `#555555` | `--color-kluup-faint` | Footer text, flip affordance hint |

### Phase 6 new: per-trait palette (6 distinct hues, dark-card harmonized)

> Source: D-05 (locked hue families). Exact hex confirmed here as the design contract.

| Trait key | Hex | Rationale |
|-----------|-----|-----------|
| `drole` (funny/witzig) | `#F59E0B` | Amber — warm, playful, legible on dark |
| `fiable` (reliable) | `#3B82F6` | Blue — calm, trustworthy |
| `audacieux` (bold) | `#EF4444` | Red — energetic, assertive |
| `empathique` (empathetic) | `#22C55E` | Green — nurturing, open |
| `mysterieux` (mysterious) | `#A855F7` | Violet — enigmatic, distinctive |
| `romantique` (romantic) | `#EC4899` | Pink — tender, emotional |

These 6 values are used **only** for: trait bar fill color + trait label color. They are never used as card background, button, or structural accent.

### 60/30/10 split for this phase

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0D0D0D` | Card background, page background |
| Secondary (30%) | `#1A1A1A` | Face 1 duo block, Face 2 archetype block, award row surfaces |
| Accent (10%) | Theme color (per `THEME_META`) | Card top color bar, group title highlight, logo accent. **Reserved for:** top 6px bar only + Kluup wordmark accent + existing stat pill backgrounds. |

**Trait hues are NOT accent** — they form a separate personality-result palette. They appear only inside the archetype block (Face 2) and must never bleed into structural chrome.

**Destructive:** the system quit/destructive button uses `--color-accent-a` = `#FF3C6F`, **not** `#EF4444`. The `#EF4444` token appears only inside the archetype trait bar in this phase (the `audacieux` hue), so there is no contextual collision. No destructive actions in Phase 6.

---

## Card Architecture — 2-Face Contract

### Face state

```
activeCard: 'group' | 'personal'   (React state, default: 'group')
```

- **Only the active face is rendered** inside the capture `<div>` (P-07 prevention). Switching `activeCard` swaps the entire child tree.
- The capture `<div>` is **540×540px** with `position: 'relative'`, `overflow: 'hidden'`. Identical dimensions to existing ShareCard.

### Face 1 — Group (default)

Renders when `activeCard === 'group'`.

```
┌── 540px ───────────────────────────────────────┐
│  [6px color bar — theme color]                 │
│  ────────────────────────────────────────────  │
│  [Theme name]              [Klu·up logo]       │  ← 28px top, 32px sides
│  ────────────────────────────────────────────  │
│  "Ce soir vous étiez…" (13px muted)            │
│  [GROUP TITLE 42px display 800]                │  ← margin-top 8px
│  ────────────────────────────────────────────  │
│  [Duo awards block — surface bg #1A1A1A]       │
│  │ "Les duos de la soirée" (13px muted ALLCAPS)│
│  │ 🧲  Magnétisme Suspicieux                   │  ← 16px C.text
│  │     Nico & Sarah           (13px muted)     │
│  │ 🧠  Même longueur d'onde                    │
│  │     Léa & Thomas           (13px muted)     │
│  │ ⚔️   Les Ennemis Jurés                       │
│  │     Jules & Paul           (13px muted)     │
│  │ 🔥  Les Complices                           │
│  │     Nico & Léa             (13px muted)     │
│  ────────────────────────────────────────────  │
│  [spacer — flex: 1]                            │
│  [Player pills]                                │
│  kluup.app (13px faint, centered)              │
└────────────────────────────────────────────────┘
```

**Edge case — fewer than 2 qualified awards (D-03):** The duo awards block is omitted entirely. Face 1 shows group title + spacer + player pills. No "no duos" placeholder text is shown; the card is simply the group title card as before.

**Duo awards block layout:**
- Container: `background: C.surface`, `borderRadius: 18`, `padding: '16px 16px'`, `borderLeft: '4px solid ${meta.color}'` (matches existing "Moment fort" block style)
- Section label: `font-size: 13px`, uppercase, letter-spacing `0.06em`, color `C.muted`, margin-bottom `8px`
- Award rows: `gap: 8px` between rows, `display: flex`, `alignItems: 'flex-start'`
- Emoji cell: `width: 32px`, `flexShrink: 0`, `fontSize: 18px` (decorative glyph)
- Award name: `fontSize: 16px`, `fontWeight: 400`, `color: C.text` (stands out vs 13px `C.muted` player names via size + color)
- Player names: `fontSize: 13px`, `color: C.muted`, on the same line after `·` separator — format: `{Pseudo1} & {Pseudo2}`

### Face 2 — Personal (archetype + existing stats)

Renders when `activeCard === 'personal'`.

```
┌── 540px ───────────────────────────────────────┐
│  [6px color bar — theme color]                 │
│  ────────────────────────────────────────────  │
│  [Theme name]              [Klu·up logo]       │
│  ────────────────────────────────────────────  │
│  [Moment fort block — existing, unchanged]     │
│  ────────────────────────────────────────────  │
│  [Personal stats pills — existing, unchanged]  │
│  ────────────────────────────────────────────  │
│  [Archetype block — surface bg #1A1A1A]        │
│  │ "Ton archétype" (13px muted ALLCAPS)        │
│  │ LE FARCEUR  (28px Bricolage 800, UPPERCASE) │
│  │ ────────────────                            │
│  │ [trait 1 colored label] ████████░░  45%     │
│  │ [trait 2 colored label] ██████░░░░  30%     │
│  │ [trait 3 colored label] ███░░░░░░░  25%     │
│  ────────────────────────────────────────────  │
│  [spacer]                                      │
│  [Player pills — existing]                     │
│  kluup.app                                     │
└────────────────────────────────────────────────┘
```

**Archetype block layout:**
- Container: same style as duo awards block (`background: C.surface`, `borderRadius: 18`, `padding: '16px 16px'`, `borderLeft: '4px solid ${meta.color}'`)
- Section label: `fr.archetypes.card_title` — 13px muted ALLCAPS letter-spacing 0.06em
- Archetype name: 28px Bricolage Grotesque weight 800, `C.text`, `textTransform: 'uppercase'`, margin-top 8px, margin-bottom 12px
- Divider: 1px `C.border`, margin-bottom 8px
- Trait rows: top 3 only. Each row: `display: flex`, `alignItems: 'center'`, `gap: 8px`, margin-bottom 8px (last row 0)
  - Trait label: `fontSize: 13px`, `fontWeight: 400`, `color: TRAIT_COLORS[traitKey]`, `width: 88px`, `flexShrink: 0` (trait hue color provides the emphasis)
  - Bar track: `height: 8px`, `flex: 1`, `background: C.border`, `borderRadius: 4`
  - Bar fill: `height: 8px`, `background: TRAIT_COLORS[traitKey]`, `borderRadius: 4`, `width: barWidthPx` (EXPLICIT PIXELS: `Math.round(pct / 100 * 160)`)
  - Percent: `fontSize: 13px`, `color: C.muted`, `width: 36px`, `textAlign: 'right'`, `flexShrink: 0`

**Edge case — total points = 0 (D-03):** Render the archetype block with the `fr.archetypes.archetype_fallback` string ("Une simple personne") in place of the archetype name. Trait rows are omitted (show nothing below the name). No "not enough data" variant — fallback is always shown when points > 0 is false.

**Edge case — fewer than 3 traits with points > 0:** Render only the traits that have points > 0 (minimum 1, maximum 3). Never render a 0-point trait bar.

---

## Flip Interaction Contract

### Tap-to-flip (D-01)

- **Trigger:** single tap/click anywhere on the card `<div>` (the 540-visual-width container in the live DOM view, not the capture container).
- **State change:** `activeCard` toggles between `'group'` and `'personal'`.
- **Animation:** CSS `opacity` fade — 150ms ease-out fade to 0, swap content, 150ms ease-in fade to 1. No 3D CSS `rotateY` (WebKit backface-visibility quirk — D-04 / P-07).
- **No swipe gesture** — tap only (avoids scroll conflict on mobile long card).

### Flip affordance (D-01 — Claude's discretion)

Displayed below the card content, inside the card wrapper but outside the capture container:

```
↻ tap to see your archetype    (Face 1)
↻ tap to see the group         (Face 2)
```

- Position: centered, below the card's bottom edge, 8px gap
- Style: 13px DM Sans weight 400, color `C.faint` (`#555555`), `cursor: pointer`
- i18n keys to add: `card.flip_to_personal` / `card.flip_to_group` (see Copywriting section)
- The affordance is rendered in the live DOM only — **never** inside the capture container

### Share button (D-02)

- Position: below the flip affordance, outside the flip surface
- Label: `fr.end.share_cta` (existing: "Partager la soirée" / "Share the night")
- Exports: `domToBlob(captureRef.current, { width: 540, height: 540, scale: 2 })` — only the currently-active face
- The capture `<div>` is a separate hidden `<div ref={captureRef}>` that renders only the active face (no flip UI, no affordance text)

---

## Copywriting Contract

All copy via `useT()` hook. Zero hardcoded strings. Keys already present in `lib/i18n.ts` are marked ✓EXISTING.

| Element | FR copy | Key | Status |
|---------|---------|-----|--------|
| Primary CTA — share | "Partager la soirée" | `end.share_cta` | ✓EXISTING |
| Face 1 section label (duos) | "Les duos de la soirée" | `duo_awards.title` | ✓EXISTING |
| Award: Magnetic | "Magnétisme Suspicieux" | `duo_awards.award_magnetisme` | ✓EXISTING |
| Award: Wavelength | "Même longueur d'onde" | `duo_awards.award_longueur_onde` | ✓EXISTING |
| Award: Enemies | "Les Ennemis Jurés" | `duo_awards.award_ennemis` | ✓EXISTING |
| Award: Accomplices | "Les Complices" | `duo_awards.award_complices` | ✓EXISTING |
| Face 2 section label | "Ton archétype" | `archetypes.card_title` | ✓EXISTING |
| Archetype fallback | "Une simple personne" | `archetypes.archetype_fallback` | ✓EXISTING |
| All 21 archetype names | (see i18n.ts `archetypes.*`) | `archetypes.archetype_*` | ✓EXISTING |
| 6 trait labels | "drôle", "fiable", "audacieux", "empathique", "mystérieux", "romantique" | `archetypes.trait_*` | ✓EXISTING |
| **Flip to personal affordance** | "↻ voir ton archétype" | `card.flip_to_personal` | **NEW — add to i18n** |
| **Flip to group affordance** | "↻ voir le groupe" | `card.flip_to_group` | **NEW — add to i18n** |

### New i18n keys (FR/EN/ES/DE — add to all 4 locales in `lib/i18n.ts` under `card:`)

```typescript
// FR
card: {
  // ...existing keys...
  flip_to_personal: "↻ voir ton archétype",
  flip_to_group: "↻ voir le groupe",
}

// EN
card: {
  flip_to_personal: "↻ see your archetype",
  flip_to_group: "↻ see the group",
}

// ES
card: {
  flip_to_personal: "↻ ver tu arquetipo",
  flip_to_group: "↻ ver el grupo",
}

// DE
card: {
  flip_to_personal: "↻ Archetyp ansehen",
  flip_to_group: "↻ Gruppe ansehen",
}
```

### Empty states

| State | What the player sees | Copy strategy |
|-------|---------------------|---------------|
| Face 1, < 2 awards qualify | Group title only (duo block omitted) | Silence — no "no duos" message. The card reads as a clean group title card. |
| Face 2, total points = 0 | Archetype block shows fallback name only | `fr.archetypes.archetype_fallback` ("Une simple personne"). No trait bars. |
| Face 2, 0 qualifying traits | Show archetype name only; omit trait section | Silent omission — do not show a "no data" message. |

### Destructive actions

None in Phase 6. No confirmations required.

---

## Component Inventory

New components to create (source: ARCHITECTURE.md):

| Component | File | Purpose | Capture-safe |
|-----------|------|---------|--------------|
| `ArchetypeBlock` | `components/ArchetypeBlock.tsx` | Archetype name + top-3 trait bars. Used inside Face 2. | Yes — explicit px widths |
| `DuoAwardsBlock` | `components/DuoAwardsBlock.tsx` | 4 named award rows. Used inside Face 1. | Yes — no % widths |

Existing components to refactor:

| Component | File | Change |
|-----------|------|--------|
| `ShareCard` | `app/room/[code]/game/page.tsx` ~L1187 | Split into Face 1 / Face 2 via `activeCard` state. Add flip wrapper and affordance text. Add `captureRef` separate from the visible card. |
| `EndScreen` | `app/room/[code]/game/page.tsx` ~L1303 | Add archetype computation (fetch myId votes + played question tags), duo awards computation (all room votes), memoize both. Feed into refactored ShareCard. |

---

## Capture Container Contract (modern-screenshot safety)

Enforced by P-07 and D-04:

| Rule | Value | Rationale |
|------|-------|-----------|
| Container dimensions | 540×540px, explicit inline `width`/`height` | Matches existing `domToBlob` call — do not change |
| Scale | 2 (1080px output) | Retina quality — matches existing |
| Background | `C.bg` = `#0D0D0D` | Explicit `backgroundColor` on `domToBlob` options |
| Trait bar widths | `Math.round(pct / 100 * 160)` px, applied as `style={{ width: barWidthPx }}` | Never `%` in capture context |
| Face rendering | Render ONLY the active face inside capture container | No `display:none` hidden face — swap entire subtree |
| Font await | `await document.fonts.ready` before `domToBlob` | Existing pattern — do not remove |
| Emoji in awards | Retain emoji (🧲🧠⚔️🔥) — existing Android/WebKit risk accepted | Test on real device before ship. Fallback: replace with Unicode text symbols if glitches found. |
| Capture trigger | `exportCard()` reads `captureRef.current` (the off-screen capture div, not the visible card) | Prevents transform:scale measurement error |

---

## Trait Palette — Final Hex Values

```typescript
export const TRAIT_COLORS: Record<string, string> = {
  drole:       '#F59E0B',   // amber
  fiable:      '#3B82F6',   // blue
  audacieux:   '#EF4444',   // red
  empathique:  '#22C55E',   // green
  mysterieux:  '#A855F7',   // violet
  romantique:  '#EC4899',   // pink
}
```

These values go in `lib/archetypes.ts` (or co-located with `ArchetypeBlock`). They are not added to `app/globals.css` — they are static constants, not design tokens.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No new npm packages. No registry components. This phase uses only existing stack: React, Tailwind v4, Supabase client, `modern-screenshot` (already installed).

---

## Accessibility Notes

- Card flip tap target: minimum 44×44px (the entire visible card satisfies this)
- Trait bars: color-only distinction is mitigated by the % label alongside each bar; acceptable for MVP
- Archetype name `textTransform: uppercase` is applied via CSS/inline style — underlying text node is mixed-case for screen reader compatibility
- Award player names: plain text, no icon-only information

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: FLAG (non-blocking — 6px color bar, justified vs existing ShareCard)
- [x] Dimension 6 Registry Safety: PASS

**Approval:** APPROVED (2026-06-14, revision 2)
