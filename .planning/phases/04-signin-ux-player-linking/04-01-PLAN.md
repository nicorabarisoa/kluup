---
phase: 04-signin-ux-player-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/i18n.ts
autonomous: true
requirements: [AUTH-01, AUTH-03]
must_haves:
  truths:
    - "fr.auth.sign_in, fr.auth.sign_out, and fr.auth.pseudo_prefilled_hint resolve to non-empty strings in all four locales"
    - "npm run build compiles cleanly (Dict type exhaustiveness satisfied across fr/en/es/de)"
  artifacts:
    - path: "lib/i18n.ts"
      provides: "auth namespace (sign_in, sign_out, pseudo_prefilled_hint) in fr/en/es/de dictionaries"
      contains: "auth:"
  key_links:
    - from: "lib/i18n.ts en/es/de dictionaries"
      to: "Dict type (typeof fr)"
      via: "structural type check at compile time"
      pattern: "auth:\\s*\\{"
---

<objective>
Add the `auth` i18n namespace (three keys) to all four locale dictionaries in `lib/i18n.ts`. This is the foundation layer for Phase 4: every auth UI string rendered on the landing and join pages reads from `fr.auth.*`. Per CLAUDE.md rule #1, zero hardcoded text — these keys must exist before the consuming pages (plans 03 and 04) can compile.

Purpose: Supply the i18n keys (AUTH-01 sign-in label, AUTH-03 sign-out label, pseudo pre-fill hint) so downstream UI plans render translated strings with no hardcoded text.
Output: `lib/i18n.ts` with an `auth` namespace in fr, en, es, and de blocks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-signin-ux-player-linking/04-CONTEXT.md
@.planning/phases/04-signin-ux-player-linking/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth namespace to all four locale dictionaries</name>
  <files>lib/i18n.ts</files>
  <read_first>
    - lib/i18n.ts (the whole file — confirm the structure: `export const fr = { ... }` at line 1, `export type Dict = typeof fr` at line 239, `export const en: Dict = {` at line 241, `export const es: Dict = {` at line 479, `export const de: Dict = {` at line 717; each locale's `join` namespace already has `pseudo_prefilled_hint` and `pseudo_taken` — the new `auth.pseudo_prefilled_hint` is a DISTINCT key, do not collapse them)
  </read_first>
  <action>
    Add an `auth` namespace object with exactly three string keys — `sign_in`, `sign_out`, `pseudo_prefilled_hint` — to EACH of the four dictionary objects (`fr`, `en`, `es`, `de`). Add it as a top-level namespace inside each dictionary (alongside `common`, `home`, `landing`, `join`, etc.), keeping each dictionary's existing key order otherwise untouched.

    Because `Dict` is defined as `typeof fr` (line 239), adding `auth` to the `fr` block automatically extends the type, and the `: Dict` annotations on `en`/`es`/`de` (lines 241/479/717) enforce that all three keys appear in those three locales too — do not skip any locale or any key.

    Exact string values per locale (from 04-UI-SPEC.md Copywriting Contract and 04-RESEARCH.md i18n Keys):
    - fr: sign_in="Se connecter", sign_out="Se déconnecter", pseudo_prefilled_hint="Nom Google pré-rempli — modifiable"
    - en: sign_in="Sign in", sign_out="Sign out", pseudo_prefilled_hint="Pre-filled from Google — you can change it"
    - es: sign_in="Iniciar sesión", sign_out="Cerrar sesión", pseudo_prefilled_hint="Nombre de Google completado — puedes cambiarlo"
    - de: sign_in="Anmelden", sign_out="Abmelden", pseudo_prefilled_hint="Google-Name vorausgefüllt — änderbar"

    Do NOT touch the existing `fr.join.pseudo_prefilled_hint` ("Ton ancien pseudo est pré-rempli.") — that is the returning-player hint and stays. The new `auth.pseudo_prefilled_hint` is a separate string for the Google-name pre-fill case.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - lib/i18n.ts contains the substring `auth:` at least 4 times (once per locale dictionary)
    - lib/i18n.ts contains `sign_in: "Se connecter"`, `sign_in: "Sign in"`, `sign_in: "Iniciar sesión"`, `sign_in: "Anmelden"`
    - lib/i18n.ts contains `sign_out: "Se déconnecter"`, `sign_out: "Sign out"`, `sign_out: "Cerrar sesión"`, `sign_out: "Abmelden"`
    - lib/i18n.ts still contains the original `pseudo_prefilled_hint: "Ton ancien pseudo est pré-rempli."` (existing join hint not overwritten)
    - `npm run build` exits 0 (Dict exhaustiveness across all four locales satisfied)
  </acceptance_criteria>
  <done>The `auth` namespace exists in fr/en/es/de with sign_in, sign_out, pseudo_prefilled_hint; build is clean; existing join hint untouched.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- New i18n namespace `auth` (object literal) in `lib/i18n.ts`, present in each of `fr`, `en`, `es`, `de`
- New i18n keys: `auth.sign_in`, `auth.sign_out`, `auth.pseudo_prefilled_hint` (all four locales)
- These keys become available on the `Dict` type via `typeof fr`, consumed through the existing `useT()` hook in plans 03 and 04
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only static dictionary strings; no input crosses any trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | lib/i18n.ts static strings | accept | Strings are compile-time constants in source control; no user input, no runtime injection surface. ASVS V5 input-validation N/A — no input added. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (per 04-RESEARCH.md Package Legitimacy Audit: "No new packages are installed in Phase 4"). |

No high-severity threats. No ASVS L1 control is triggered by static-string additions.
</threat_model>

<verification>
- `npm run build` exits 0 (TypeScript + Dict exhaustiveness gate).
- The three `auth.*` keys are present and non-empty in all four locale blocks.
</verification>

<success_criteria>
- `auth.sign_in`, `auth.sign_out`, `auth.pseudo_prefilled_hint` resolve in fr/en/es/de.
- Build is clean; existing `join.pseudo_prefilled_hint` is preserved.
</success_criteria>

<output>
Create `.planning/phases/04-signin-ux-player-linking/04-01-SUMMARY.md` when done
</output>
