---
phase: 05-stats-persistence-profile
plan: 05
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - lib/utils.ts
  - lib/i18n.ts
  - app/room/[code]/game/page.tsx
  - app/PendingStatsFlusher.tsx
  - app/layout.tsx
autonomous: true
requirements: [PROF-02, STAT-01]

must_haves:
  truths:
    - "Tapping the end-screen sign-in CTA stashes the full save payload (user-agnostic: session_uuid, theme, rounds_played, my designated/confessed/volunteered counts, group_title, code) into localStorage BEFORE the OAuth redirect navigates away"
    - "After completing Google sign-in — even after a multi-minute delay that lets the solo room get swept — the SIGNED_IN event flushes the stashed payload to user_session_stats, independent of whether the room row still exists"
    - "The flush is idempotent: it upserts with onConflict user_id,session_id ignoreDuplicates and clears the stash on success, so a second SIGNED_IN or a page revisit does not double-write or re-flush"
    - "After a successful flush the user sees a localized 'Stats sauvegardées ✓' confirmation (toast) wherever they landed (game page or home), never a silent disappearance"
    - "When the game page mounts and the room is gone (init() room-not-found) but a pending-stats stash exists for this code, the user is NOT silently bounced — the flush still runs and the confirmation shows before/instead of the bare push('/')"
  artifacts:
    - path: "lib/utils.ts"
      provides: "setPendingStats / getPendingStats / clearPendingStats helpers backed by a single localStorage key (kluup_pending_stats), SSR-safe with try/catch like the existing player-id helpers"
      contains: "kluup_pending_stats"
    - path: "app/PendingStatsFlusher.tsx"
      provides: "Global 'use client' component: on mount getUser() + onAuthStateChange(SIGNED_IN) → read stash → upsert to user_session_stats → clear stash → show localized confirmation toast"
      contains: "user_session_stats"
    - path: "app/layout.tsx"
      provides: "PendingStatsFlusher mounted once inside LocaleProvider so the flush fires no matter which page the OAuth round-trip lands on"
      contains: "PendingStatsFlusher"
    - path: "lib/i18n.ts"
      provides: "save_prompt.flushed (or end.stats_saved reuse) confirmation string present in all four locales"
      contains: "flushed"
  key_links:
    - from: "EndScreen handleCTASignIn"
      to: "localStorage kluup_pending_stats"
      via: "setPendingStats(payload) called before supabase.auth.signInWithOAuth"
      pattern: "setPendingStats"
    - from: "PendingStatsFlusher onAuthStateChange SIGNED_IN"
      to: "user_session_stats table"
      via: "supabase.upsert(getPendingStats(), { onConflict: 'user_id,session_id', ignoreDuplicates: true })"
      pattern: "user_session_stats"
    - from: "game page init() room-not-found branch"
      to: "PendingStatsFlusher / stash"
      via: "stash survives even though the room row was swept; flusher runs globally regardless of router.push('/')"
      pattern: "getPendingStats"
---

<objective>
Close the major UAT gap: an anonymous player who taps the end-screen "Se connecter" CTA and takes several minutes on the Google sign-in screen lands back on `/` (home) with their session stats lost forever. Root cause (diagnosed in `.planning/debug/oauth-return-lands-on-home.md`): `signInWithOAuth` is a full-page redirect that destroys all React state; in a solo room the presence heartbeat stops and the pg_cron sweep deletes the room (cascading `game_state.session_uuid` + `votes`) during the multi-minute sign-in; on return the game page's `init()` finds no room and the silent guard at `app/room/[code]/game/page.tsx` (`if (!roomData) { router.push('/'); return }`) fires before `EndScreen` and its retroactive-save effect can ever mount.

This plan implements the **most robust** of the three diagnosed fix directions (fix direction 1): stash the entire save payload in `localStorage` BEFORE the redirect, and flush it on `SIGNED_IN` from a globally-mounted client component — making the save completely independent of room lifetime and of which page the OAuth round-trip lands on. It also handles fix direction 3 (no silent bounce) as a consequence: the global flusher runs regardless of the game page's `router.push('/')`, and the user always sees a confirmation. The SQL TTL exemption (fix direction 2) is handled separately in plan 05-06 as defense-in-depth.

Purpose: PROF-02's promise ("sign in to save YOUR stats") must be honored at the hottest conversion moment even when the room dies during a slow OAuth — currently it is silently broken.
Output: `lib/utils.ts` (stash helpers), `app/PendingStatsFlusher.tsx` (new global flusher), `app/layout.tsx` (mount it), `lib/i18n.ts` (confirmation string), `app/room/[code]/game/page.tsx` (CTA stashes payload + flusher import path note).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-stats-persistence-profile/05-UAT.md
@.planning/debug/oauth-return-lands-on-home.md
@.planning/phases/05-stats-persistence-profile/05-CONTEXT.md
@CLAUDE.md
@lib/utils.ts
@lib/types.ts
@app/auth/callback/route.ts
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pending-stats stash helpers to lib/utils.ts and the confirmation i18n string</name>
  <files>lib/utils.ts, lib/i18n.ts</files>
  <action>
In `lib/utils.ts`, add three exported helpers backed by a single localStorage key `kluup_pending_stats` (declare it as a module-level const alongside the existing `PID_PREFIX`/`LAST_ROOM_KEY`/`LAST_PSEUDO_PREFIX` consts). Mirror the SSR-safety and try/catch style of the existing `getPlayerId`/`setPlayerId`/`clearPlayerId` helpers exactly (guard `typeof window === 'undefined'`, wrap every localStorage call in try/catch):

- Define and export a `PendingStats` type (the user-agnostic save payload, all the columns of `user_session_stats` EXCEPT `user_id` which is only known after sign-in): `{ session_id: string; designated_count: number; confessed_count: number; volunteered_count: number; group_title: string; theme: string; rounds_played: number; code: string }`. `tag_scores` is intentionally omitted from the stash and supplied as `{}` at flush time (D-08 dormant — keep consistent with the existing write effect at `app/room/[code]/game/page.tsx` which writes `tag_scores: {}`).
- `setPendingStats(p: PendingStats): void` — `localStorage.setItem('kluup_pending_stats', JSON.stringify(p))`, try/catch.
- `getPendingStats(): PendingStats | null` — read + `JSON.parse`, return null on any failure (missing key, bad JSON). SSR-guard.
- `clearPendingStats(): void` — `localStorage.removeItem('kluup_pending_stats')`, try/catch.

Rationale to document in a short comment block above the const (match the file's existing comment density): this stash survives the full-page `signInWithOAuth` redirect that destroys React state, and survives room deletion by the pg_cron sweep — so the save no longer depends on the `rooms.game_state` row still existing on return. Everything needed for the upsert is small and known BEFORE the CTA is tapped.

In `lib/i18n.ts`, add ONE new key `flushed` to the existing `save_prompt` namespace in ALL FOUR locale objects (fr at ~line 232, en ~497, es ~760, de ~1023). The `Dict` type forces exhaustiveness — adding to one locale without the others fails `tsc`. Reuse the existing wording of `end.stats_saved` per locale for consistency (fr: "Stats sauvegardées ✓", en: "Stats saved ✓", es: "Estadísticas guardadas ✓", de: "Stats gespeichert ✓"). Do NOT hardcode any string in the flusher component — it must read this key. (Zero-hardcoded-text rule, CLAUDE.md.)
  </action>
  <verify>
    <automated>node -e "const u=require('fs').readFileSync('lib/utils.ts','utf8'); if(!/kluup_pending_stats/.test(u)) process.exit(1); for(const f of ['setPendingStats','getPendingStats','clearPendingStats','PendingStats']) if(!new RegExp(f).test(u)){console.error('missing '+f);process.exit(1)}; const i=require('fs').readFileSync('lib/i18n.ts','utf8'); if((i.match(/flushed:/g)||[]).length<4){console.error('flushed not in all 4 locales');process.exit(1)}; console.log('ok')"</automated>
  </verify>
  <done>lib/utils.ts exports setPendingStats/getPendingStats/clearPendingStats and a PendingStats type keyed on kluup_pending_stats; save_prompt.flushed exists in all four locales; `npx tsc --noEmit` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create the global PendingStatsFlusher and mount it; make the CTA stash before redirect</name>
  <files>app/PendingStatsFlusher.tsx, app/layout.tsx, app/room/[code]/game/page.tsx</files>
  <behavior>
    - On SIGNED_IN with a pending stash present: exactly one upsert to user_session_stats fires with user_id=session.user.id and the stashed fields, tag_scores:{}, onConflict 'user_id,session_id' ignoreDuplicates true; on success the stash is cleared and a localized confirmation toast shows.
    - On SIGNED_IN with no pending stash: no upsert, no toast (no-op).
    - On a fresh mount where the user is ALREADY signed in (getUser returns a user) AND a stash exists (e.g. the SIGNED_IN event already fired during the callback before this component mounted): the same flush runs once. Guard against double-flush within the component (a ref/flag) so getUser + SIGNED_IN don't both fire it.
    - A second SIGNED_IN or remount after a successful flush does not re-flush (stash already cleared) and does not double-write (ignoreDuplicates + cleared stash).
  </behavior>
  <action>
Create `app/PendingStatsFlusher.tsx` as a `'use client'` component that renders only a transient confirmation toast (or null). Implementation:

- Imports: `useEffect`, `useRef`, `useState` from react; `supabase` from `@/lib/supabase`; `getPendingStats`, `clearPendingStats` from `@/lib/utils`; `useT` from `@/lib/locale`.
- A single `useEffect([])` that:
  1. Defines an idempotent `flush(userId: string)` that: reads `getPendingStats()`; if null, returns; otherwise calls `supabase.from('user_session_stats').upsert({ user_id: userId, session_id: p.session_id, designated_count: p.designated_count, confessed_count: p.confessed_count, volunteered_count: p.volunteered_count, group_title: p.group_title, theme: p.theme, rounds_played: p.rounds_played, tag_scores: {} }, { onConflict: 'user_id,session_id', ignoreDuplicates: true })`; on no-error, `clearPendingStats()` and set a `setShowToast(true)` state (auto-hide after ~4s via setTimeout). On error, `console.error('[pending stats flush]', error)` and DO NOT clear the stash (so a later retry can succeed). Use a `flushingRef` boolean guard so getUser() and the SIGNED_IN listener cannot run `flush` concurrently/twice.
  2. Calls `supabase.auth.getUser().then(({ data }) => { if (data.user) flush(data.user.id) })` — covers the case where the session was already established by the `/auth/callback` route before this component mounted.
  3. Subscribes `supabase.auth.onAuthStateChange((event, session) => { if (event === 'SIGNED_IN' && session?.user) flush(session.user.id) })` and unsubscribes in cleanup.
- The toast: a fixed-position element (top or bottom center) styled to match the project tokens (dark surface, small rounded pill) showing `useT().save_prompt.flushed`. Keep it minimal and self-dismissing; it must not block interaction. Do NOT hardcode the string — read `save_prompt.flushed` via `useT()`.

This component must NOT import anything room-specific and must be safe to mount on every route.

In `app/layout.tsx`, import `PendingStatsFlusher` and render it once inside `<LocaleProvider>`, as a sibling to `{children}` (e.g. `<LocaleProvider><PendingStatsFlusher />{children}</LocaleProvider>`). It needs `useT()` so it MUST be inside the provider. The layout stays a server component — importing a `'use client'` child is allowed in the App Router.

In `app/room/[code]/game/page.tsx`, modify `handleCTASignIn` (currently around line 1417, inside `EndScreen`) so that BEFORE calling `supabase.auth.signInWithOAuth`, it stashes the payload:
- Import `setPendingStats` (and the `PendingStats` type if helpful) from `@/lib/utils` at the top of the file (the file already imports `getPlayerId, clearPlayerId` from there — extend that import).
- Compute the same values the existing write effect computes: `const totalRounds = gs.stats.rounds_a + gs.stats.rounds_b + gs.stats.rounds_c; const titleKey = computeGroupTitle(gs.stats, theme, totalRounds)` (both `gs`/`theme`/`computeGroupTitle` are already in scope inside EndScreen — `titleKey`/`totalRounds` are already computed a few lines below; reuse or recompute consistently). Read the per-player counts exactly as the write effect does: `(gs.stats.designated ?? {})[myId ?? ''] ?? 0`, same for `confessed`/`volunteered`.
- Call `setPendingStats({ session_id: gs.session_uuid, designated_count, confessed_count, volunteered_count, group_title: titleKey, theme, rounds_played: totalRounds, code })` BEFORE `signInWithOAuth`. Guard: only stash if `gs.session_uuid` is truthy (no point stashing an empty session id).
- Leave the existing `redirectTo: \`${window.location.origin}/auth/callback?next=${encodeURIComponent('/room/' + code + '/game')}\`` UNCHANGED — the CLAUDE.md OAuth gotcha still applies (always route through `/auth/callback?next=<path>`).

Do NOT remove the existing in-EndScreen retroactive write effect (lines ~1318-1343) — it still handles the happy path where the room survives. The flusher is an independent, additive safety net. The two are idempotent against each other via `onConflict ... ignoreDuplicates` on the same `(user_id, session_id)`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/PendingStatsFlusher.tsx','utf8'); for(const t of [\"'use client'\",'getPendingStats','clearPendingStats','user_session_stats','onAuthStateChange','SIGNED_IN','save_prompt']) if(!c.includes(t)){console.error('flusher missing '+t);process.exit(1)}; const l=fs.readFileSync('app/layout.tsx','utf8'); if(!/PendingStatsFlusher/.test(l)){console.error('not mounted in layout');process.exit(1)}; const g=fs.readFileSync('app/room/[code]/game/page.tsx','utf8'); if(!/setPendingStats\\(/.test(g)){console.error('CTA does not stash');process.exit(1)}; if(!/auth\\/callback\\?next=/.test(g)){console.error('OAuth callback route changed');process.exit(1)}; console.log('ok')"</automated>
  </verify>
  <done>app/PendingStatsFlusher.tsx exists (client component, flushes stash on SIGNED_IN to user_session_stats, idempotent, localized toast); it is mounted once inside LocaleProvider in app/layout.tsx; handleCTASignIn calls setPendingStats before signInWithOAuth with redirectTo still via /auth/callback?next=; `npx tsc --noEmit` and `npm run build` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → user_session_stats upsert | The stashed payload is attacker-controllable client data; it is written under the signed-in user's own RLS scope |
| anonymous client → Supabase Auth (OAuth) | Untrusted full-page redirect round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Tampering | localStorage stash forged with arbitrary counts/session_id | accept | Stats are vanity/non-authoritative (CLAUDE.md: "nudge, pas coffre-fort"); the upsert is RLS-scoped to `auth.uid() = user_id` so a user can only write their OWN rows; no privilege or money attached. Same trust model as the existing client-computed write effect. |
| T-05-16 | Spoofing | flush writes user_id from session.user.id (server-validated) not from stash | mitigate | user_id is taken from `session.user.id` (Auth-validated) / `getUser()`, never from the stash; the stash carries no user_id. RLS INSERT policy `auth.uid() = user_id` blocks cross-user writes. |
| T-05-17 | Info disclosure | stash persisting in localStorage after flush | mitigate | `clearPendingStats()` on successful flush; only one short-lived payload, no secrets/PII (pseudo not stored, only counts + theme + group_title key). |
| T-05-18 | DoS | repeated SIGNED_IN events causing duplicate writes | mitigate | `flushingRef` guard + `ignoreDuplicates` on `UNIQUE(user_id, session_id)` + stash cleared on success → at most one effective write per session. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (Dict exhaustiveness for `save_prompt.flushed` across all 4 locales; new helper types compile).
- `npm run build` passes (`/` and `/room/[code]/game` still build; layout renders the client flusher).
- Manual (covered by re-running UAT test 2): anonymous game → end screen → tap CTA → wait 2-3 min on Google → complete sign-in. Regardless of whether the user lands on `/room/{code}/game` or `/`, the flusher fires on SIGNED_IN, the row appears in `user_session_stats`, the "Stats sauvegardées ✓" toast shows, and the session subsequently appears in `/profile` history.
- Grep guard: `app/room/[code]/game/page.tsx` still routes OAuth through `/auth/callback?next=` (CLAUDE.md gotcha not regressed).
</verification>

<success_criteria>
- The end-screen CTA stashes the full save payload to `localStorage` (`kluup_pending_stats`) before the OAuth redirect.
- A globally-mounted `PendingStatsFlusher` upserts the stash to `user_session_stats` on `SIGNED_IN` (and on mount-while-already-signed-in), idempotently, clearing the stash and showing a localized confirmation — independent of room survival and of which page the round-trip lands on.
- The silent loss path (room swept during slow OAuth → `push('/')` → stats gone) no longer loses stats: the save now completes on the home page if that's where the user lands.
- No hardcoded UI text; `save_prompt.flushed` present in all four locales.
- OAuth still routes through `/auth/callback?next=<path>` (no CLAUDE.md regression).
</success_criteria>

<output>
Create `.planning/phases/05-stats-persistence-profile/05-05-SUMMARY.md` when done.
</output>
