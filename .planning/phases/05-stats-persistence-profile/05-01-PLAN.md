---
phase: 05-stats-persistence-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/i18n.ts
  - supabase/migrations/005-stats-columns.sql
  - supabase/schema.sql
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, PROF-01, PROF-02]
user_setup: []

must_haves:
  truths:
    - "The build compiles with the new profile.* and save_prompt.* keys present in all four locales (fr/en/es/de)"
    - "A migration file exists that adds theme, rounds_played, tag_scores to user_session_stats without touching any RLS policy"
    - "schema.sql contains the three new columns both in the CREATE TABLE block and as an idempotent ALTER, with the user_session_stats RLS block unchanged"
  artifacts:
    - path: "supabase/migrations/005-stats-columns.sql"
      provides: "Additive idempotent migration for theme/rounds_played/tag_scores"
      contains: "ADD COLUMN IF NOT EXISTS tag_scores"
    - path: "supabase/schema.sql"
      provides: "Source-of-truth mirror of the three new columns"
      contains: "tag_scores"
    - path: "lib/i18n.ts"
      provides: "profile.* and save_prompt.* namespaces in all four locales"
      contains: "save_prompt"
  key_links:
    - from: "supabase/schema.sql"
      to: "supabase/migrations/005-stats-columns.sql"
      via: "identical column definitions (theme text, rounds_played int, tag_scores jsonb)"
      pattern: "ADD COLUMN IF NOT EXISTS theme"
---

<objective>
Lay the data + i18n foundation for Phase 5: write the additive DB migration (and mirror it into the idempotent source-of-truth `schema.sql`), and add the `profile.*` and `save_prompt.*` i18n namespaces to all four locales.

Purpose: Every downstream plan (DB apply, game-page write/CTA, profile page, landing chip) consumes these artifacts. The migration files must exist before the DB apply (Plan 02); the i18n keys must exist before any UI references them (Plans 03/04). The `Dict` type forces locale exhaustiveness — adding keys to one locale only breaks the build, so all four land in this single plan.
Output: `supabase/migrations/005-stats-columns.sql` (new), `supabase/schema.sql` (modified — three columns mirrored), `lib/i18n.ts` (modified — 14 keys × 4 locales).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stats-persistence-profile/05-CONTEXT.md
@.planning/phases/05-stats-persistence-profile/05-RESEARCH.md
@.planning/phases/05-stats-persistence-profile/05-PATTERNS.md
@.planning/phases/05-stats-persistence-profile/05-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the additive migration file and mirror it into schema.sql</name>
  <files>supabase/migrations/005-stats-columns.sql, supabase/schema.sql</files>
  <read_first>
    - supabase/migrations/002-auth.sql (exact additive-migration pattern to copy: header comment block + ADD COLUMN IF NOT EXISTS, NO policy touches)
    - supabase/schema.sql (lines 59-78 = user_session_stats CREATE TABLE block; lines 141-155 = user_session_stats RLS block which MUST NOT be touched)
    - .planning/phases/05-stats-persistence-profile/05-PATTERNS.md (sections "supabase/migrations/005-stats-columns.sql" and "supabase/schema.sql")
    - CLAUDE.md §"Modèle de données (Supabase)" and §"gotchas" (host_id NOT NULL note: CREATE TABLE IF NOT EXISTS is a no-op on an existing table, so an ALTER is required for prod)
  </read_first>
  <action>
    Create `supabase/migrations/005-stats-columns.sql` following the 002-auth.sql header style (per D-06). Header comment states: additive-only, safe on live prod, existing rows unaffected (new columns NULL), does NOT touch any RLS policy. Body is a single statement: `ALTER TABLE user_session_stats ADD COLUMN IF NOT EXISTS theme text, ADD COLUMN IF NOT EXISTS rounds_played int, ADD COLUMN IF NOT EXISTS tag_scores jsonb;`. Columns are nullable with NO DEFAULT (pre-Phase-5 rows stay NULL — profile renders "—" / omits the chip; avoids Pitfall 4).

    Mirror into `supabase/schema.sql` per the Phase 2 convention (every migration mirrored idempotently). Two edits: (1) add the three columns inside the existing `CREATE TABLE IF NOT EXISTS user_session_stats (...)` block (after `group_title text,` / before `played_at`) so a fresh provision gets them; (2) add an `ALTER TABLE user_session_stats ADD COLUMN IF NOT EXISTS theme text, ADD COLUMN IF NOT EXISTS rounds_played int, ADD COLUMN IF NOT EXISTS tag_scores jsonb;` immediately after that CREATE TABLE block (because `CREATE TABLE IF NOT EXISTS` is a no-op on the already-provisioned prod table — same reasoning as the host_id gotcha). Do NOT modify the `stats_select_own` / `stats_insert_own` / `stats_update_own` policy block (lines ~141-155) or any other RLS block — this is the silent-lockout / Pitfall 6 regression guard.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('supabase/migrations/005-stats-columns.sql','utf8');const s=fs.readFileSync('supabase/schema.sql','utf8');const needCols=['theme','rounds_played','tag_scores'];const mig=needCols.every(c=>new RegExp('ADD COLUMN IF NOT EXISTS '+c).test(m));const schAlter=needCols.every(c=>new RegExp('ADD COLUMN IF NOT EXISTS '+c).test(s));const noPolicyInMig=!/POLICY/i.test(m);const policyIntact=/CREATE POLICY \"stats_insert_own\"/.test(s)&&/auth.uid\(\) = user_id/.test(s);if(mig&&schAlter&&noPolicyInMig&&policyIntact){console.log('OK')}else{console.error('FAIL',{mig,schAlter,noPolicyInMig,policyIntact});process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/005-stats-columns.sql` exists and contains `ADD COLUMN IF NOT EXISTS theme`, `ADD COLUMN IF NOT EXISTS rounds_played`, `ADD COLUMN IF NOT EXISTS tag_scores`.
    - The migration file contains the word `POLICY` zero times (it touches no RLS).
    - `supabase/schema.sql` contains all three `ADD COLUMN IF NOT EXISTS` statements after the user_session_stats CREATE TABLE block AND the three columns inside the CREATE TABLE block.
    - `supabase/schema.sql` still contains `CREATE POLICY "stats_insert_own"` and the `auth.uid() = user_id` check unchanged.
    - All three new columns are nullable with no DEFAULT clause.
  </acceptance_criteria>
  <done>Migration file written (additive, no policy touch); schema.sql mirrors the columns in both CREATE TABLE and ALTER form; user_session_stats RLS block untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add profile.* and save_prompt.* i18n namespaces to all four locales</name>
  <files>lib/i18n.ts</files>
  <read_first>
    - lib/i18n.ts (line 143 `end:` namespace pattern incl. arrow-function plurals like `rounds_played: (n) => ...` and `stat_designated: (n) => ...`; line 186 `archetypes:` namespace; line 237 `auth:`; line 244 `export type Dict = typeof fr`; line 975 `dictionaries` — and the matching positions in en ~388, es ~631, de ~874)
    - .planning/phases/05-stats-persistence-profile/05-UI-SPEC.md §"Copywriting Contract" (exact FR/EN/ES/DE strings for all 14 keys including the arrow-function plural form of `profile.sessions_played`)
    - .planning/phases/05-stats-persistence-profile/05-PATTERNS.md §"lib/i18n.ts"
  </read_first>
  <action>
    Add two new namespaces to EACH of the four locale objects (`fr`, `en`, `es`, `de`), using the exact strings from the UI-SPEC Copywriting Contract. Place them adjacent to the existing `end:` / `archetypes:` namespaces for readability.

    `profile` namespace (11 keys): `title`, `sessions_played` (arrow function `(n: number) => ...` with locale plural rules per UI-SPEC — mirror the existing `end.rounds_played` function-value style), `stat_designated_total`, `stat_confessed_total`, `stat_volunteered_total`, `history_title`, `empty_heading`, `empty_body`, `empty_cta`, `nav_link`, `error_load`.

    `save_prompt` namespace (3 keys): `heading`, `body`, `cta`.

    Do NOT add any archetype or trait keys — the `archetypes` namespace already has all 6 traits + 22 archetype names + `card_title` + `archetype_fallback` (reuse them in Plan 04). Do NOT alter the `Dict` type line or the `dictionaries` satisfies line — they pick up the new keys automatically. The build (`tsc`/`next build`) fails if any locale is missing a key (Dict exhaustiveness) — that is the safety net.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "^$" | head -20; node -e "const s=require('fs').readFileSync('lib/i18n.ts','utf8');const profCount=(s.match(/title:|sessions_played:|stat_designated_total:|stat_confessed_total:|stat_volunteered_total:|history_title:|empty_heading:|empty_body:|empty_cta:|nav_link:|error_load:/g)||[]).length;const saveCount=(s.match(/save_prompt:/g)||[]).length;if(saveCount>=4 && (s.match(/sessions_played:/g)||[]).length>=4){console.log('OK profile/save_prompt present in 4 locales')}else{console.error('FAIL counts', {saveCount, sessions:(s.match(/sessions_played:/g)||[]).length});process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/i18n.ts` contains a `save_prompt:` namespace object 4 times (once per locale).
    - `sessions_played:` appears at least 4 times (once per locale, as an arrow function).
    - `npx tsc --noEmit` reports no errors (Dict exhaustiveness satisfied across all four locales).
    - No new keys added to the `archetypes` namespace (it is reused as-is).
    - All 11 `profile.*` keys and 3 `save_prompt.*` keys present in fr/en/es/de matching the UI-SPEC strings.
  </acceptance_criteria>
  <done>profile.* (11 keys) and save_prompt.* (3 keys) present in all four locales; build/type-check passes; archetypes namespace untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL author → live prod DB | The migration text in this plan is applied to prod in Plan 02; a wrong DROP/CREATE here cascades into a silent RLS lockout |
| Build pipeline → all UI | Missing locale key breaks the typed Dict and fails the build (fail-closed, not silent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | user_session_stats RLS policies | mitigate | Migration file touches NO policy (`POLICY` count = 0 asserted in verify); schema.sql edit limited to CREATE TABLE + ALTER ADD COLUMN, RLS block left byte-identical — prevents the "Room introuvable" silent-lockout regression (Pitfall 6) |
| T-05-02 | Tampering | schema.sql idempotency | mitigate | `ADD COLUMN IF NOT EXISTS` only (no DROP COLUMN, no DEFAULT backfill) — re-running schema.sql on prod is a no-op on existing columns (Pitfall 4) |
| T-05-03 | Information Disclosure | i18n strings | accept | All copy is non-secret UI text; no PII, no tokens |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; RESEARCH Package Legitimacy Audit confirms zero new dependencies for Phase 5 |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (Dict exhaustiveness for the 14 new keys across 4 locales).
- Migration file contains the three `ADD COLUMN IF NOT EXISTS` and zero `POLICY` references.
- schema.sql mirrors the columns and leaves the user_session_stats RLS block unchanged.
</verification>

<success_criteria>
- Three new columns (`theme`, `rounds_played`, `tag_scores`) defined in BOTH the migration file and schema.sql, idempotently, with no RLS change (D-06, SC-6 foundation).
- `profile.*` + `save_prompt.*` namespaces complete in fr/en/es/de; build green (foundation for PROF-01, PROF-02 UI).
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (whole-phase, across plans 01-04)

> Listed once here (in the wave-1 foundation plan) so downstream executors and the checker have a single inventory of every new symbol Phase 5 introduces.

### Database (live prod + schema.sql + migration)
- New file `supabase/migrations/005-stats-columns.sql` (Plan 01)
- `user_session_stats.theme text` — column (Plan 01 def, Plan 02 apply)
- `user_session_stats.rounds_played int` — column (Plan 01 def, Plan 02 apply)
- `user_session_stats.tag_scores jsonb` — column (Plan 01 def, Plan 02 apply)

### i18n (lib/i18n.ts — all four locales fr/en/es/de)
- New namespace `profile.*` (11 keys): `title`, `sessions_played` (fn), `stat_designated_total`, `stat_confessed_total`, `stat_volunteered_total`, `history_title`, `empty_heading`, `empty_body`, `empty_cta`, `nav_link`, `error_load` (Plan 01)
- New namespace `save_prompt.*` (3 keys): `heading`, `body`, `cta` (Plan 01)
- New key `end.stats_saved` (the "✓ saved" receipt line) (Plan 03)
- Reused (no new keys): `archetypes.*` (traits + 22 archetypes + card_title + archetype_fallback), `end.stat_*`, `home.themes.*`, `titles.*`

### New routes / pages
- New route `app/profile/page.tsx` → `/profile` (Plan 04, client component)

### New components (all inline — no shared components/ folder, per project convention)
- `SignInSaveCTA` — inline in `app/room/[code]/game/page.tsx` (Plan 03)
- `ArchetypeBlock`, `CumulativeStatsGrid`, `SessionHistoryList`, `SessionRow` — inline in `app/profile/page.tsx` (Plan 04)
- `PrimaryBtn`, `GhostBtn`, `PlayerAvatar` — re-defined inline in `app/profile/page.tsx` (copied, not imported)

### New behavior wired into existing files
- Game-page root auth state (`user`) + single `onAuthStateChange` listener carrying `TOKEN_REFRESHED`→`supabase.realtime.setAuth` (SC-5) and `SIGNED_IN`→retroactive save + silent `players.user_id` link (Plan 03)
- Idempotent `user_session_stats` upsert effect on the `ended` phase (Plan 03)
- Landing name chip → `/profile` navigation (Plan 04)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-stats-persistence-profile/05-01-SUMMARY.md` when done
</output>
