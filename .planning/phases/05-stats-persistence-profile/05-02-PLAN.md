---
phase: 05-stats-persistence-profile
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false
requirements: [STAT-01, STAT-02, STAT-03, PROF-01]
user_setup: []

must_haves:
  truths:
    - "The live prod Supabase user_session_stats table has columns theme, rounds_played, and tag_scores"
    - "The anonymous game flow still works after the migration (no RLS lockout regression)"
  artifacts: []
  key_links:
    - from: "supabase/migrations/005-stats-columns.sql"
      to: "live Supabase DB (project dmxjspnrrgcixzcthgwf)"
      via: "manual paste into the Supabase SQL editor"
      pattern: "ADD COLUMN IF NOT EXISTS"
---

<objective>
Apply the Phase 5 migration (`supabase/migrations/005-stats-columns.sql`) to the live production Supabase database so the `theme`, `rounds_played`, and `tag_scores` columns exist before any app code writes them.

Purpose: [BLOCKING] The build and type checks pass WITHOUT this apply (Supabase types here are hand-written, not generated from the live DB), creating a false-positive verification state. The app code in Plan 03 that upserts `theme`/`rounds_played`/`tag_scores` will throw at runtime against an un-migrated prod DB. This project applies SQL MANUALLY via the Supabase SQL editor — it does NOT use the Supabase CLI `db push` (confirmed by Phase 2/3 history). Therefore this is a human-action checkpoint.
Output: live prod DB columns present; AUTH-04 anonymous-regression smoke confirmed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-stats-persistence-profile/05-CONTEXT.md
@.planning/phases/05-stats-persistence-profile/05-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration to the live Supabase DB</name>
  <read_first>
    - supabase/migrations/005-stats-columns.sql (the exact SQL to paste)
    - CLAUDE.md §"gotchas" (scripts run in the Supabase SQL editor; NEXT_PUBLIC_* are build-time only)
    - .planning/phases/05-stats-persistence-profile/05-RESEARCH.md §"Common Pitfalls" (Pitfall 4 idempotent ADD COLUMN, Pitfall 6 RLS silent lockout)
  </read_first>
  <what-built>
    Plan 01 wrote `supabase/migrations/005-stats-columns.sql` — an additive, idempotent migration adding `theme text`, `rounds_played int`, `tag_scores jsonb` to `user_session_stats`, touching no RLS policy. It has NOT yet been applied to the live prod DB. The CLI is not used here; the apply is a manual SQL-editor paste.
  </what-built>
  <how-to-verify>
    1. Open the Supabase dashboard for project `dmxjspnrrgcixzcthgwf` → SQL Editor → New query.
    2. Paste the FULL contents of `supabase/migrations/005-stats-columns.sql` and Run. Expected: "Success. No rows returned." (it is additive DDL).
    3. Confirm the columns exist — run:
       `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'user_session_stats' ORDER BY ordinal_position;`
       Expected: the result set includes `theme` (text, YES), `rounds_played` (integer, YES), `tag_scores` (jsonb, YES).
    4. Confirm RLS is intact — run:
       `SELECT policyname FROM pg_policies WHERE tablename = 'user_session_stats';`
       Expected: `stats_select_own`, `stats_insert_own`, `stats_update_own` all present.
    5. AUTH-04 anonymous regression smoke (mandatory after any schema/RLS-adjacent change): in a fresh/incognito browser (NOT signed in), open the deployed app, create a room, join from a second tab with a different pseudo, play through at least one Type A, one Type B, and one Type C round, reach the end screen, and confirm "Rejouer" returns to the lobby. Expected: no "Room introuvable", no missing players.
  </how-to-verify>
  <verify>
    <human-check>Steps 3 and 4 SQL return the three new columns and the three intact policies; step 5 anonymous game completes a full round of each type without "Room introuvable".</human-check>
  </verify>
  <acceptance_criteria>
    - `information_schema.columns` for `user_session_stats` includes `theme`, `rounds_played`, `tag_scores` (all nullable).
    - `pg_policies` for `user_session_stats` still lists `stats_select_own`, `stats_insert_own`, `stats_update_own`.
    - Full anonymous game (create → join → A/B/C → end → replay) completes with no regression.
  </acceptance_criteria>
  <resume-signal>Type "applied" once the three columns are confirmed present, the three RLS policies are intact, and the anonymous game smoke passes. Describe any error otherwise.</resume-signal>
  <done>Live prod DB has theme/rounds_played/tag_scores; user_session_stats RLS unchanged; anonymous flow regression-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator → live prod DB | Manual SQL paste into the prod SQL editor — wrong script or partial run can lock out anonymous players |
| Migrated DB → anonymous game | RLS on rooms/players/votes must remain open after the apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | rooms/players/votes anon RLS | mitigate | Migration is column-only DDL, touches no policy; mandatory AUTH-04 anonymous smoke test (step 5) catches any accidental lockout before sign-off |
| T-05-05 | Tampering | user_session_stats scoped RLS | mitigate | Step 4 explicitly re-verifies the three `*_own` policies survive the apply |
| T-05-06 | Repudiation | migration provenance | accept | Single-operator solo project; apply recorded in SUMMARY + STATE.md per Phase 2/3 convention |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; SQL-only apply |
</threat_model>

<verification>
- Columns present via `information_schema.columns` query.
- RLS policies intact via `pg_policies` query.
- Anonymous game smoke (AUTH-04) passes end-to-end.
</verification>

<success_criteria>
- Live prod `user_session_stats` has `theme`, `rounds_played`, `tag_scores` (SC-6).
- No RLS regression; anonymous flow unaffected (AUTH-04 gate).
</success_criteria>

<output>
Create `.planning/phases/05-stats-persistence-profile/05-02-SUMMARY.md` when done
</output>
