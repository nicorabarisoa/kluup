---
phase: 03-playtest-quality-fixes
plan: 05
type: execute
wave: 2
depends_on: ["03-02", "03-03"]
files_modified:
  - app/join/page.tsx
  - app/room/[code]/lobby/page.tsx
autonomous: true
requirements: [SC-1, SC-3, SC-4, SC-6]
must_haves:
  truths:
    - "A duplicate pseudo join shows an inline error under the input, not an alert"
    - "The error clears when the user edits the pseudo or the code"
    - "Rejoining after quitting pre-populates the old pseudo in the editable input, requiring submit"
    - "The lobby has a Quitter button with the same behaviour as in-game (no confirm dialog)"
    - "Quitting as the last player in the lobby deletes the room"
  artifacts:
    - path: "app/join/page.tsx"
      provides: "23505 inline pseudo error + rejoin pre-population"
      contains: "pseudo_taken"
    - path: "app/room/[code]/lobby/page.tsx"
      provides: "lobby Quitter button reusing in-game onQuit logic"
      contains: "clearPlayerId"
  key_links:
    - from: "app/join/page.tsx"
      to: "idx_players_pseudo_lower (Plan 03)"
      via: "supabase insert error.code === '23505'"
      pattern: "23505"
    - from: "app/room/[code]/lobby/page.tsx"
      to: "rooms / players delete"
      via: "onQuit: delete player, delete room if last, else transfer host"
      pattern: "clearPlayerId"
---

<objective>
Wire the two user-facing join/lobby fixes. In `app/join/page.tsx`: catch Postgres `23505` from the pseudo unique index (Plan 03) and show an inline error (D-01/D-02), and pre-populate the old pseudo on rejoin while still requiring an explicit submit (D-14). In `app/room/[code]/lobby/page.tsx`: add a Quitter button that mirrors the in-game `onQuit` logic — host transfer or room deletion — with NO confirmation dialog (D-13).

Purpose: SC-1 (duplicate-pseudo message visible to the user), SC-4 (fresh editable pseudo on rejoin), SC-6 (lobby quit button), SC-3 (last-player lobby quit deletes the room).
Output: Updated `app/join/page.tsx` and `app/room/[code]/lobby/page.tsx`.

NOTE: Runs in Wave 2 after Plan 02 (i18n keys `pseudo_taken`, `pseudo_prefilled_hint`) and Plan 03 (the `idx_players_pseudo_lower` index — without it, the 23505 error never fires). No file overlap with Plan 04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playtest-quality-fixes/03-CONTEXT.md
@.planning/phases/03-playtest-quality-fixes/03-RESEARCH.md
@.planning/phases/03-playtest-quality-fixes/03-PATTERNS.md
@.planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md
@lib/i18n.ts
@lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline pseudo-taken error on duplicate join (D-01, D-02, D-03)</name>
  <files>app/join/page.tsx</files>
  <read_first>
    - app/join/page.tsx (state at lines 12–14; the player insert + error block at lines 60–74 where `if (playerError || !player)` currently alerts; the pseudo `<input>` at lines 116–124 with its `onChange`; the join `<button>` at lines 125–133)
    - lib/i18n.ts (the `join.pseudo_taken` key from Plan 02)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "app/join/page.tsx" #1 — exact error-state pattern and the 23505 branch)
    - .planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md ("Pseudo Already Taken Error" — inline 12px text, color rgba(255,60,111,0.85), below input, clears on change)
    - .planning/phases/03-playtest-quality-fixes/03-RESEARCH.md (Pattern 2 + Pitfall 6 — the reconnect path must NOT hit the constraint for the player's own name)
  </read_first>
  <action>
    In `app/join/page.tsx`:
    (1) Add state: `const [pseudoError, setPseudoError] = useState<string | null>(null)` next to the existing `code`/`pseudo`/`loading` state.
    (2) Replace the current insert-error block (lines 67–72, `if (playerError || !player) { console.error(...); alert(fr.join.join_error); ... }`) with branch logic: `if (playerError) { if (playerError.code === '23505') { setPseudoError(fr.join.pseudo_taken) } else { console.error('[joinRoom] player insert failed:', playerError); alert(fr.join.join_error) } setLoading(false); return } if (!player) { setLoading(false); return }`. This shows the inline error only for the unique-violation code 23505 and keeps the existing `alert` for other errors. Do NOT alter the reconnect path at lines 52–58 — when the stored `player_id` still exists in the DB, the code reuses it and never reaches the insert, so a reconnecting player never hits the constraint on their own name (Pitfall 6).
    (3) Clear the error on input: change the pseudo input `onChange` (line 120) to `onChange={(e) => { setPseudo(e.target.value); setPseudoError(null) }}` and the code input `onChange` (line 111) to also call `setPseudoError(null)` alongside the existing `setCode(...)`.
    (4) Render the inline error: below the pseudo `<input>` and above the join `<button>`, add `{pseudoError && (<p className="text-xs text-center" style={{ color: 'rgba(255,60,111,0.85)', fontFamily: 'var(--font-body)' }}>{pseudoError}</p>)}`. Use 12px (`text-xs`), no background/border/icon — plain inline text per UI-SPEC. The submit button stays enabled so the user can retype and resubmit.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/join/page.tsx` declares `pseudoError` state (`useState<string | null>`)
    - The insert-error handler branches on `playerError.code === '23505'` and calls `setPseudoError(fr.join.pseudo_taken)`
    - The pseudo input and code input `onChange` handlers both call `setPseudoError(null)`
    - An inline `{pseudoError && (<p ...>{pseudoError}</p>)}` renders below the pseudo input with `color: 'rgba(255,60,111,0.85)'` and `text-xs`
    - The reconnect block at lines 52–58 (`getPlayerId` + existing-row reuse) is unchanged
    - `npm run build` passes
  </acceptance_criteria>
  <done>A duplicate (case-insensitive) pseudo join surfaces `fr.join.pseudo_taken` as inline text under the input; the error clears on edit; reconnecting players are unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-populate old pseudo on rejoin (D-14)</name>
  <files>app/join/page.tsx</files>
  <read_first>
    - app/join/page.tsx (the `useEffect` reading `searchParams.get('code')` at lines 17–20; the reconnect lookup at lines 52–58; `getPlayerId` import at line 6)
    - lib/utils.ts (`getPlayerId(code)` reads `localStorage` key `kluup_pid_<CODE>`)
    - lib/i18n.ts (the `join.pseudo_prefilled_hint` key from Plan 02)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "app/join/page.tsx" #2 — the storedPseudo state + useEffect pattern that fetches the pseudo by stored id)
    - .planning/phases/03-playtest-quality-fixes/03-CONTEXT.md (D-14 — pre-populate the old name but editable; player MUST confirm/submit)
    - .planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md ("Pre-Populated Pseudo on Rejoin" — fully editable, optional hint text)
  </read_first>
  <action>
    In `app/join/page.tsx`, add rejoin pre-population:
    (1) Add `const [storedPseudo, setStoredPseudo] = useState<string | null>(null)`.
    (2) In the mount effect that reads `searchParams.get('code')`, after setting the code, read `const pid = getPlayerId(upperCode)` (where `upperCode` is the uppercased code). If `pid` exists, query `supabase.from('players').select('pseudo').eq('id', pid).maybeSingle()` and, if a pseudo comes back, `setStoredPseudo(data.pseudo)` and `setPseudo(data.pseudo)` to pre-fill the editable input. Keep this defensive (no crash if the row is gone — `maybeSingle` returns null). Follow the exact pattern in 03-PATTERNS.md §"app/join/page.tsx" #2.
    (3) Render an optional hint when the field is still showing the pre-filled value: below the pseudo input (above or near the `pseudoError` line), add `{storedPseudo && pseudo === storedPseudo && (<p className="text-xs" style={{ color: '#888', fontFamily: 'var(--font-body)' }}>{fr.join.pseudo_prefilled_hint}</p>)}`. The input stays fully editable; the user still must tap "Rejoindre" — the pseudo is never silently reused (D-14). The existing disabled-state on the button (empty pseudo blocks submit) is preserved.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/join/page.tsx` declares `storedPseudo` state
    - The mount effect reads `getPlayerId(...)` and, when a stored id is found, queries `players.select('pseudo')...maybeSingle()` and calls `setPseudo(...)` to pre-fill
    - A hint `{storedPseudo && pseudo === storedPseudo && (<p ...>{fr.join.pseudo_prefilled_hint}</p>)}` is rendered
    - The pseudo input remains editable (no `readOnly`/`disabled`) and the join button still requires a non-empty pseudo
    - `npm run build` passes
  </acceptance_criteria>
  <done>On rejoin after quitting, the old pseudo pre-fills the editable input with a hint; the user must submit to join — it is never silently reused.</done>
</task>

<task type="auto">
  <name>Task 3: Lobby Quitter button reusing in-game quit logic (D-13)</name>
  <files>app/room/[code]/lobby/page.tsx</files>
  <read_first>
    - app/room/[code]/lobby/page.tsx (imports at lines 1–10 — `getPlayerId` is imported, `clearPlayerId` is NOT; state `players`/`myId`/`roomId` at lines 20–22; `isHost` derivation at line 122; the header row with `LangSwitch` at lines 164–166; the centered column wrapper `max-w-md mx-auto` at line 163)
    - app/room/[code]/game/page.tsx (the `onQuit` function at lines 1816–1837 — the canonical logic: clearPlayerId, delete own player, fetch remaining, delete room if 0 left, else promote oldest if was host, router.push('/'))
    - lib/utils.ts (`clearPlayerId(code)` clears the per-room localStorage identity)
    - lib/i18n.ts (the existing `game.quit` key — "Quitter"/"Leave"/"Salir"/"Verlassen")
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "app/room/[code]/lobby/page.tsx" — the onQuit copy, the roomId difference, the button placement pattern)
    - .planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md ("Lobby Quit Button" — px-3 h-8 rounded-xl, C.surface bg, C.border, #888 text, top-left, NO confirm dialog)
  </read_first>
  <action>
    In `app/room/[code]/lobby/page.tsx`:
    (1) Add `clearPlayerId` to the existing import from `@/lib/utils` (currently `import { copyToClipboard, getPlayerId } from '@/lib/utils'` → add `clearPlayerId`).
    (2) Add an `onQuit` async function adapted from the game page's `onQuit` (lines 1816–1837). The lobby has `roomId` (string state), not a full `room` object, so use `roomId` for the room/players queries: guard `if (!roomId || !myId) { router.push('/'); return }`; capture `const wasHost = isHost`; `clearPlayerId(code)`; `await supabase.from('players').delete().eq('id', myId)`; fetch `const { data: rest } = await supabase.from('players').select().eq('room_id', roomId)`; `const remaining = (rest ?? []) as Player[]`; if `remaining.length === 0` → `await supabase.from('rooms').delete().eq('id', roomId)` (SC-3: last player deletes the room); else if `wasHost` → promote the oldest remaining by `created_at` via `supabase.from('players').update({ is_host: true }).eq('id', next.id)`; then `router.push('/')`. Do NOT wrap it in `window.confirm` — the lobby quit is a single immediate tap per D-13 and UI-SPEC.
    (3) Add the button to the header row. Change the existing `<div className="w-full flex justify-end px-5 pt-4">` (line 164, which holds only `LangSwitch`) to `justify-between`, and add, as the first child (left), a `<button type="button" onClick={onQuit} className="text-xs font-medium px-3 h-8 rounded-xl" style={{ background: C.surface, border: \`1px solid ${C.border}\`, color: C.muted, fontFamily: 'var(--font-body)' }}>{fr.game.quit}</button>`, keeping `<LangSwitch />` on the right. Use the existing `C` constant (defined at line 155) for colors. `type="button"` prevents any accidental form submit.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/room/[code]/lobby/page.tsx` imports `clearPlayerId` from `@/lib/utils`
    - An `onQuit` function exists that calls `clearPlayerId(code)`, deletes the player by `myId`, deletes the room when `remaining.length === 0`, and promotes the oldest remaining player when the quitter was host
    - `onQuit` does NOT call `window.confirm`
    - A `<button type="button" onClick={onQuit} ...>{fr.game.quit}</button>` is rendered in the header row, styled `px-3 h-8 rounded-xl` with `C.surface`/`C.border`/`C.muted`
    - The header row uses `justify-between` with the quit button on the left and `LangSwitch` on the right
    - `npm run build` passes
  </acceptance_criteria>
  <done>The lobby shows a Quitter button (top-left, no confirm) that deletes the player, transfers host or deletes the empty room, and returns home — matching in-game behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → players/rooms delete | A lobby quit deletes player rows and possibly the room; RLS is open (MVP) so this matches existing in-game quit behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | duplicate pseudo bypass at join | mitigate | Enforced by the DB unique index from Plan 03 (`idx_players_pseudo_lower`); the join page only surfaces the 23505 error — it is not the enforcement point, so a client cannot bypass uniqueness by skipping the UI. |
| T-03-04 | Repudiation | lobby quit deletes another player's row | accept | RLS is open in the MVP (CLAUDE.md); the lobby quit deletes only `myId` (own row) and follows the exact existing in-game pattern. No new exposure beyond the already-shipped in-game quit. |
</threat_model>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift verification):
- `pseudoError` and `storedPseudo` state (local to the join page component)
- `onQuit` function in `app/room/[code]/lobby/page.tsx` (new local function; mirrors the existing game-page `onQuit`)
- New import of existing `clearPlayerId` into the lobby page
- No new files, exported functions, or components.
</artifacts_this_phase_produces>

<verification>
- `npm run build` passes after all three tasks.
- Manual (per 03-VALIDATION.md): two tabs joining the same room with same-case-different pseudo → second sees inline error (SC-1); rejoin after quit shows editable pre-filled pseudo (SC-4); lobby Quitter button present and functional, last-player quit deletes the room (SC-6, SC-3).
</verification>

<success_criteria>
- Duplicate pseudo → inline `fr.join.pseudo_taken` error, clears on edit (SC-1).
- Rejoin pre-populates the editable pseudo, requires submit (SC-4).
- Lobby has a no-confirm Quitter button with full host-transfer / room-deletion logic (SC-6, SC-3).
- Build green.
</success_criteria>

<output>
Create `.planning/phases/03-playtest-quality-fixes/03-05-SUMMARY.md` when done.
</output>
