---
phase: 03-playtest-quality-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/i18n.ts
  - lib/usePresence.ts
autonomous: true
requirements: [SC-2, SC-3, SC-9]
must_haves:
  truths:
    - "All 4 language dictionaries declare a join.pseudo_taken error string"
    - "All 4 language dictionaries declare a game.player_joined(pseudo) function-valued string"
    - "All 4 language dictionaries declare a join.pseudo_prefilled_hint string"
    - "The landing players hint reads 'recommended/conseillé' phrasing in all 4 languages"
    - "The presence grace period is 15s and the heartbeat is 30s"
    - "lib/usePresence.ts prune callback deletes the room when the last player is pruned (count === 0)"
  artifacts:
    - path: "lib/i18n.ts"
      provides: "pseudo_taken, player_joined, pseudo_prefilled_hint keys + updated players_hint"
      contains: "pseudo_taken"
    - path: "lib/usePresence.ts"
      provides: "GRACE_MS=15000, HEARTBEAT_MS=30000, room deletion after last player prune"
      contains: "15_000"
  key_links:
    - from: "lib/i18n.ts"
      to: "Dict type"
      via: "exhaustive key set across fr/en/es/de"
      pattern: "player_joined"
---

<objective>
Land all string-only and constant-only leaf changes that downstream plans consume: the new i18n keys (D-02 pseudo-taken, D-10 player_joined toast, D-14 rejoin hint), the landing copy fix (D-15), and the presence-grace/heartbeat constant changes (D-04/D-06). These files have zero overlap with the game/join/lobby pages, so this plan runs in parallel with Plans 01 and 03.

Purpose: SC-9 (landing copy), SC-2 (15s grace + screen-lock survival), and the i18n contract that Plans 04 and 05 depend on.
Output: Updated `lib/i18n.ts` and `lib/usePresence.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playtest-quality-fixes/03-CONTEXT.md
@.planning/phases/03-playtest-quality-fixes/03-PATTERNS.md
@.planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new i18n keys to all 4 language dictionaries (D-02, D-10, D-14)</name>
  <files>lib/i18n.ts</files>
  <read_first>
    - lib/i18n.ts (the `join` section, the `game` section with existing `quit`/`quit_confirm` keys, and the `landing` section with `players_hint`; the `Dict` type enforces every language has identical keys)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "lib/i18n.ts" — the exact key table and the function-valued key pattern)
    - .planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md ("Copywriting Contract" table — canonical translations)
  </read_first>
  <action>
    In `lib/i18n.ts`, add three new keys to the `join` and `game` sections of ALL four language dictionaries (`fr`, `en`, `es`, `de`). The `Dict` type will fail the build until every language has every key, so add to all four. Keys and canonical values (use the UI-SPEC Copywriting Contract translations, which take precedence over PATTERNS where they differ):
    (1) `join.pseudo_taken` (plain string) — FR: "Ce pseudo est déjà pris, choisis-en un autre." · EN: "That name is already taken — pick another one." · ES: "Ese nombre ya está en uso, elige otro." · DE: "Dieser Name ist schon vergeben, wähle einen anderen."
    (2) `join.pseudo_prefilled_hint` (plain string) — FR: "Ton ancien pseudo est pré-rempli." · EN: "Your previous name has been pre-filled." · ES: "Tu nombre anterior está rellenado." · DE: "Dein vorheriger Name wurde vorausgefüllt."
    (3) `game.player_joined` (function-valued, signature `(pseudo: string) => string`) — follow the existing function-valued key pattern already used in the `lobby` section (e.g. `need_players`). FR: `(p) => ` + backtick `${p} a rejoint la partie` · EN: `(p) => ` backtick `${p} joined the game` · ES: `(p) => ` backtick `${p} se unió a la partida` · DE: `(p) => ` backtick `${p} ist beigetreten`.
    Place `pseudo_taken` and `pseudo_prefilled_hint` inside the existing `join: { ... }` object (next to `room_not_found`/`join_error`); place `player_joined` inside the existing `game: { ... }` object (next to `quit`/`quit_confirm`). Do NOT add a new section.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `lib/i18n.ts` contains `pseudo_taken` exactly 4 times (once per language)
    - `lib/i18n.ts` contains `pseudo_prefilled_hint` exactly 4 times
    - `lib/i18n.ts` contains `player_joined` exactly 4 times, each a function `(pseudo: string) => ...` (or `(p) => ...`)
    - `npm run build` passes with no `Dict` exhaustiveness TypeScript error
  </acceptance_criteria>
  <done>Three new i18n keys exist in all four language dictionaries; `game.player_joined` is function-valued; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Update landing players_hint copy in all 4 languages (D-15)</name>
  <files>lib/i18n.ts</files>
  <read_first>
    - lib/i18n.ts (the `landing.players_hint` lines: FR ~line 22, EN ~line 205, ES ~line 386, DE ~line 567)
    - .planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md ("Landing Page Copy Fix" section — exact before/after strings)
  </read_first>
  <action>
    In `lib/i18n.ts`, replace the value of `players_hint` in each of the four `landing` sections. New values (from UI-SPEC §"Landing Page Copy Fix"): FR: "Conseillé entre 3 et 10 joueurs · 10–20 min" · EN: "Recommended 3–10 players · 10–20 min" · ES: "Recomendado entre 3 y 10 jugadores · 10–20 min" · DE: "Empfohlen 3–10 Spieler · 10–20 Min". Keep the ` · 10–20 min` segment (with the en-dash in 10–20, matching the existing string). Change only the leading "X à/to/a/bis 10 ..." segment to the "Conseillé/Recommended/Recomendado/Empfohlen ..." phrasing. Do NOT touch `app/page.tsx` — it already renders `fr.landing.players_hint`; this is a string-only change in the dictionary.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `lib/i18n.ts` no longer contains the substring "3 à 10 joueurs"
    - `lib/i18n.ts` contains "Conseillé entre 3 et 10 joueurs" (FR)
    - `lib/i18n.ts` contains "Recommended 3–10 players" (EN)
    - `lib/i18n.ts` contains "Recomendado entre 3 y 10 jugadores" (ES)
    - `lib/i18n.ts` contains "Empfohlen 3–10 Spieler" (DE)
    - `npm run build` passes
  </acceptance_criteria>
  <done>All four `players_hint` values use the recommended-phrasing; the old "3 à 10 joueurs" string is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce presence grace to 15s and heartbeat to 30s (D-04, D-06)</name>
  <files>lib/usePresence.ts</files>
  <read_first>
    - lib/usePresence.ts (constants at lines 8 and 11: `GRACE_MS = 60_000` and `HEARTBEAT_MS = 120_000`; the prune `setTimeout(..., GRACE_MS)` at line 46-51; the `setInterval(bump, HEARTBEAT_MS)` at line 66)
    - .planning/phases/03-playtest-quality-fixes/03-CONTEXT.md (D-04, D-06, and the §specifics note about phone-lock within 15s)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "lib/usePresence.ts" — only the two constant values change, not the structure)
  </read_first>
  <action>
    In `lib/usePresence.ts`, make the following changes:
    (1) Change `const GRACE_MS = 60_000` to `const GRACE_MS = 15_000`. Update the comment to: "15s grace (D-04/D-06): covers a phone screen-lock + unlock within 15s without pruning the player; a closed tab is pruned after 15s."
    (2) Change `const HEARTBEAT_MS = 120_000` to `const HEARTBEAT_MS = 30_000`. Update the comment to note 30s keeps presence signal fresher per CONTEXT.md §specifics.
    (3) After the existing `await supabase.from('players').delete().eq('id', key)` line inside the prune `setTimeout` callback, add room auto-deletion (D-05): `const { count } = await supabase.from('players').select('id', { count: 'exact', head: true }).eq('room_id', roomId); if (count === 0) { await supabase.from('rooms').delete().eq('id', roomId); }` — `roomId` is already in closure scope as a parameter of `useRoomPresence`. Do NOT change the `setTimeout`/`setInterval`/`isCleaner` logic structure beyond these additions.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `lib/usePresence.ts` contains `const GRACE_MS = 15_000`
    - `lib/usePresence.ts` contains `const HEARTBEAT_MS = 30_000`
    - `lib/usePresence.ts` no longer contains `60_000` or `120_000`
    - `lib/usePresence.ts` contains `count === 0` and `supabase.from('rooms').delete()` inside the prune setTimeout callback (room auto-deletion on last-player-pruned)
    - The `if (channel.presenceState()[key]) return` reconnect guard is unchanged in structure
    - `npm run build` passes
  </acceptance_criteria>
  <done>Grace period is 15s and heartbeat is 30s; prune callback deletes the room when the last player is removed; build is green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift verification):
- i18n keys: `join.pseudo_taken`, `join.pseudo_prefilled_hint`, `game.player_joined` (function `(pseudo: string) => string`) — added to `fr`, `en`, `es`, `de` in `lib/i18n.ts`
- `landing.players_hint` is an existing key with a changed value. `GRACE_MS` and `HEARTBEAT_MS` are existing constants with changed values.
- Room auto-deletion block (new code inside existing prune setTimeout callback): checks remaining player count and deletes room if 0 — no new exported symbol, added inline in `lib/usePresence.ts`
</artifacts_this_phase_produces>

<verification>
- `npm run build` passes (Dict exhaustiveness confirms all 4 languages have the 3 new keys).
- Grep confirms the new strings and the absence of the old "3 à 10 joueurs" / `60_000` / `120_000`.
</verification>

<success_criteria>
- All 4 dictionaries have `pseudo_taken`, `pseudo_prefilled_hint`, `player_joined`.
- Landing hint uses the recommended phrasing in all 4 languages (SC-9).
- Grace = 15s, heartbeat = 30s (SC-2 foundation).
- Build green.
</success_criteria>

<output>
Create `.planning/phases/03-playtest-quality-fixes/03-02-SUMMARY.md` when done.
</output>
