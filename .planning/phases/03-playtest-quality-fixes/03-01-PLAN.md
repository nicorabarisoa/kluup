---
phase: 03-playtest-quality-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/game.ts
autonomous: true
requirements: [SC-5, SC-8]
must_haves:
  truths:
    - "GameState carries an ISO timestamp for when the current voting phase started"
    - "GameState carries a snapshot of the player count taken when the voting phase started"
    - "A fresh game state defaults both new fields to safe zero values that callers overwrite"
  artifacts:
    - path: "lib/types.ts"
      provides: "round_started_at and vote_round_player_count fields on GameState"
      contains: "round_started_at"
    - path: "lib/game.ts"
      provides: "makeInitialGameState initialises the two new fields to '' and 0"
      contains: "vote_round_player_count"
  key_links:
    - from: "lib/game.ts"
      to: "lib/types.ts"
      via: "makeInitialGameState return type GameState"
      pattern: "round_started_at"
---

<objective>
Add the two GameState fields that the timer-refresh (D-07) and mid-round-join-threshold (D-09) fixes depend on: `round_started_at: string` and `vote_round_player_count: number`. This plan only extends the type contract and the initial-state factory — it does NOT wire the fields into the game page (that is Plan 04). Splitting the contract first lets Plan 04 implement against a stable type.

Purpose: Establish the data-model foundation for refresh-safe timers and correct mid-round vote thresholds (SC-5, SC-8). These are the only two GameState additions in the phase.
Output: Extended `GameState` type and updated `makeInitialGameState`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playtest-quality-fixes/03-CONTEXT.md
@.planning/phases/03-playtest-quality-fixes/03-RESEARCH.md
@.planning/phases/03-playtest-quality-fixes/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add round_started_at and vote_round_player_count to GameState</name>
  <files>lib/types.ts</files>
  <read_first>
    - lib/types.ts (the full `GameState` type ends at line 82 with `session_uuid: string`)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "lib/types.ts" — exact placement rule)
  </read_first>
  <action>
    In `lib/types.ts`, add two fields to the `GameState` type immediately after the existing `session_uuid: string` field (currently line 81), so they are the last two members of the type. Add `round_started_at: string` with a comment: "ISO timestamp set at each voting phase start by the caller (startGame/onNextRound/resolveVotes); '' for non-voting phases. Clients derive remaining timer time from it after a refresh (D-07)." Add `vote_round_player_count: number` with a comment: "Snapshot of players.length taken when the voting phase started; mid-round joiners are excluded from the current round's threshold (D-09). 0 means 'use players.length fallback' for in-flight games created before Phase 3." Do NOT change any other field. Both fields are required (not optional) — runtime code uses `|| players.length` and a `round_started_at ?` truthiness guard to handle in-flight games whose jsonb lacks the fields.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `lib/types.ts` `GameState` type contains a member `round_started_at: string`
    - `lib/types.ts` `GameState` type contains a member `vote_round_player_count: number`
    - Both new members appear after `session_uuid` (they are the last two fields of the type)
    - `npm run build` completes without TypeScript errors caused by `lib/types.ts` (note: build will surface an error in `lib/game.ts` until Task 2 is done — run Task 2 before relying on a green build)
  </acceptance_criteria>
  <done>GameState declares `round_started_at: string` and `vote_round_player_count: number` as its final two fields, with explanatory comments.</done>
</task>

<task type="auto">
  <name>Task 2: Initialise the new fields in makeInitialGameState</name>
  <files>lib/game.ts</files>
  <read_first>
    - lib/game.ts (`makeInitialGameState` is at lines 78–97; the returned object ends with `session_uuid: ''` at line 82-ish inside the return)
    - lib/types.ts (the updated GameState from Task 1 — confirm the two field names)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "lib/game.ts" — the critical constraint NOT to compute real values here)
    - .planning/phases/03-playtest-quality-fixes/03-RESEARCH.md (Pattern 4 note: makeInitialGameState cannot know players.length)
  </read_first>
  <action>
    In `lib/game.ts`, in the object returned by `makeInitialGameState(candidates: Question[])`, add `round_started_at: ''` and `vote_round_player_count: 0` immediately after the existing `session_uuid: ''` line. Use the literal zero values ONLY — do NOT call `new Date()` and do NOT reference any player list here. `makeInitialGameState` runs synchronously without access to the roster, so the real values are set by the callers (`startGame` in the lobby, and `onNextRound`/`resolveVotes` in the game page — wired in later plans). Add an inline comment on each: `round_started_at: '', // caller (startGame/onNextRound/resolveVotes) sets the real timestamp at phase start` and `vote_round_player_count: 0, // caller sets the real snapshot; 0 triggers the players.length fallback`. This keeps `makeInitialGameState` returning a valid `GameState` so the build type-checks.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `lib/game.ts` `makeInitialGameState` return object includes `round_started_at: ''`
    - `lib/game.ts` `makeInitialGameState` return object includes `vote_round_player_count: 0`
    - Neither new line calls `new Date(` nor references `players` inside `makeInitialGameState`
    - `npm run build` completes with no TypeScript errors (GameState is now fully satisfied)
  </acceptance_criteria>
  <done>`makeInitialGameState` returns a GameState whose two new fields are the literal `''` and `0`, and `npm run build` passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift verification):
- `GameState.round_started_at` (new field, type `string`) in `lib/types.ts`
- `GameState.vote_round_player_count` (new field, type `number`) in `lib/types.ts`
- No new functions, hooks, components, or files — only two type fields plus their initialisation in the existing `makeInitialGameState`.
</artifacts_this_phase_produces>

<verification>
- `npm run build` passes (TypeScript confirms GameState is fully satisfied by makeInitialGameState).
- The two fields exist on the type and are initialised in the factory.
</verification>

<success_criteria>
- GameState type has `round_started_at: string` and `vote_round_player_count: number` as final fields.
- `makeInitialGameState` initialises both to `''` / `0` without computing real values.
- Build is green.
</success_criteria>

<output>
Create `.planning/phases/03-playtest-quality-fixes/03-01-SUMMARY.md` when done.
</output>
