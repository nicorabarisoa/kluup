---
phase: 03-playtest-quality-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/003-pseudo-unique.sql
  - supabase/schema.sql
autonomous: false
requirements: [SC-1]
user_setup:
  - service: supabase
    why: "The pseudo-uniqueness index must be applied to the live Postgres database; Claude cannot run SQL against the Supabase project"
    dashboard_config:
      - task: "Run supabase/migrations/003-pseudo-unique.sql in the SQL editor"
        location: "Supabase Dashboard (project ref dmxjspnrrgcixzcthgwf) -> SQL Editor"
must_haves:
  truths:
    - "A unique index enforces case-insensitive pseudo uniqueness scoped to room_id"
    - "The migration is idempotent — re-running it does not error"
    - "schema.sql (source of truth) also declares the index idempotently"
  artifacts:
    - path: "supabase/migrations/003-pseudo-unique.sql"
      provides: "CREATE UNIQUE INDEX idx_players_pseudo_lower ON players (room_id, LOWER(pseudo))"
      contains: "idx_players_pseudo_lower"
    - path: "supabase/schema.sql"
      provides: "the same idempotent index block in the source of truth"
      contains: "idx_players_pseudo_lower"
  key_links:
    - from: "app/join/page.tsx (Plan 05)"
      to: "idx_players_pseudo_lower"
      via: "insert into players rejected with Postgres error 23505 on duplicate LOWER(pseudo)"
      pattern: "23505"
---

<objective>
Create the case-insensitive per-room pseudo-uniqueness constraint (D-01, D-03) as a PostgreSQL expression unique index, written both as a new migration file (`003-pseudo-unique.sql`) and idempotently into the source-of-truth `schema.sql` (D-discretion). Because the index must be applied to the live Supabase database and Claude cannot execute SQL there, this plan ends with a blocking human-action checkpoint to run the migration. The index is what makes the Plan 05 join-error (23505) actually fire.

Purpose: SC-1 — two players cannot join a room with the same pseudo (case-insensitive); server is the single source of truth (D-01).
Output: `supabase/migrations/003-pseudo-unique.sql`, updated `supabase/schema.sql`, and the index live in the DB.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playtest-quality-fixes/03-CONTEXT.md
@.planning/phases/03-playtest-quality-fixes/03-RESEARCH.md
@.planning/phases/03-playtest-quality-fixes/03-PATTERNS.md
@supabase/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 003-pseudo-unique.sql migration</name>
  <files>supabase/migrations/003-pseudo-unique.sql</files>
  <read_first>
    - supabase/migrations/002-auth.sql (existing migration in this directory — match its header-comment and idempotency style)
    - supabase/schema.sql (the existing idempotent index lines at 173–175: `CREATE INDEX IF NOT EXISTS idx_rooms_last_activity ...` and `idx_players_room_id ...`; the `DO $$ ... EXCEPTION ... END $$;` publication blocks at 161–171 for the idempotent-DDL pattern)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "supabase/migrations/003-pseudo-unique.sql" — exact DO-block guard)
    - .planning/phases/03-playtest-quality-fixes/03-RESEARCH.md (Pattern 1 + Pitfall 1: must use CREATE UNIQUE INDEX, NOT ALTER TABLE ADD CONSTRAINT, because Postgres rejects expression constraints)
  </read_first>
  <action>
    Create `supabase/migrations/003-pseudo-unique.sql`. Open with a comment header explaining: this enforces per-room case-insensitive pseudo uniqueness (D-01/D-03); it uses `CREATE UNIQUE INDEX` (not `ALTER TABLE ADD CONSTRAINT`) because PostgreSQL does not support expression-based unique constraints via ALTER TABLE. The body is an idempotent `DO $$ BEGIN ... END $$;` block that checks `IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE tablename = 'players' AND indexname = 'idx_players_pseudo_lower')` and, only then, runs `CREATE UNIQUE INDEX idx_players_pseudo_lower ON players (room_id, LOWER(pseudo));`. The index name MUST be exactly `idx_players_pseudo_lower` and the index expression MUST be `(room_id, LOWER(pseudo))` so uniqueness is scoped per room (two different rooms can each have a "Nico") and case-insensitive ("Nico" == "nico"). Do NOT add an `ON CONFLICT` clause and do NOT drop/recreate; the IF NOT EXISTS guard makes it safe to re-run.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/003-pseudo-unique.sql','utf8'); const ok = s.includes('idx_players_pseudo_lower') && s.includes('LOWER(pseudo)') && s.includes('room_id') && s.includes('CREATE UNIQUE INDEX') && s.includes('IF NOT EXISTS') && !s.includes('ADD CONSTRAINT'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/003-pseudo-unique.sql` exists
    - It contains `CREATE UNIQUE INDEX idx_players_pseudo_lower ON players (room_id, LOWER(pseudo))`
    - It contains an `IF NOT EXISTS` guard referencing `pg_indexes` and `idx_players_pseudo_lower`
    - It does NOT contain `ADD CONSTRAINT`
  </acceptance_criteria>
  <done>The migration file creates the expression unique index idempotently, scoped to `(room_id, LOWER(pseudo))`, using `CREATE UNIQUE INDEX`.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the index into the source-of-truth schema.sql</name>
  <files>supabase/schema.sql</files>
  <read_first>
    - supabase/schema.sql (lines 173–181: the existing `CREATE INDEX IF NOT EXISTS` lines and the trailing diagnostic comment block)
    - supabase/migrations/003-pseudo-unique.sql (the DO-block written in Task 1 — copy the same idempotent block verbatim)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (note: "Also update supabase/schema.sql — add this block after the existing idx_players_room_id line")
  </read_first>
  <action>
    In `supabase/schema.sql`, after the existing line `CREATE INDEX IF NOT EXISTS idx_players_room_id ON players (room_id);` (line 175) and before the diagnostic comment block (line 177), insert the SAME idempotent `DO $$ BEGIN IF NOT EXISTS (... idx_players_pseudo_lower ...) THEN CREATE UNIQUE INDEX idx_players_pseudo_lower ON players (room_id, LOWER(pseudo)); END IF; END $$;` block from Task 1, preceded by a one-line comment in French matching the file's style, e.g. `-- Phase 03 : unicité du pseudo par room (insensible à la casse).`. schema.sql is the source of truth (CLAUDE.md) and must stay runnable end-to-end as a single idempotent script, so the block must be safe to run on a database that already has the index.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/schema.sql','utf8'); const ok = s.includes('idx_players_pseudo_lower') && s.includes('LOWER(pseudo)') && s.includes('CREATE UNIQUE INDEX'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/schema.sql` contains `idx_players_pseudo_lower`
    - `supabase/schema.sql` contains `CREATE UNIQUE INDEX idx_players_pseudo_lower ON players (room_id, LOWER(pseudo))`
    - The new block sits after the `idx_players_room_id` index line and before the diagnostic comment block
  </acceptance_criteria>
  <done>schema.sql declares the same idempotent unique-index block, keeping it the single source of truth.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Apply the migration to the live Supabase database</name>
  <files>supabase/migrations/003-pseudo-unique.sql</files>
  <action>
    Human-only step — Claude cannot run SQL against the Supabase project. The human applies `supabase/migrations/003-pseudo-unique.sql` to the live database via the Supabase SQL editor, after running the duplicate pre-flight check. Follow the numbered steps in <how-to-verify>. This task gates the phase: the `idx_players_pseudo_lower` index must exist in production before the Plan 05 join-error path (Postgres 23505) can fire and SC-1 can be verified end-to-end.
  </action>
  <what-built>
    `supabase/migrations/003-pseudo-unique.sql` and the mirrored block in `supabase/schema.sql` create a case-insensitive per-room unique index `idx_players_pseudo_lower` on `players (room_id, LOWER(pseudo))`. Claude cannot run SQL against the Supabase project, so a human must apply it.
  </what-built>
  <how-to-verify>
    1. Open the Supabase Dashboard for project ref `dmxjspnrrgcixzcthgwf` -> SQL Editor.
    2. IMPORTANT (pre-flight): If the live `players` table already has duplicate pseudos in the same room (case-insensitive), `CREATE UNIQUE INDEX` will FAIL. First run this check and resolve any rows it returns before applying the migration:
       `SELECT room_id, LOWER(pseudo) AS p, COUNT(*) FROM players GROUP BY room_id, LOWER(pseudo) HAVING COUNT(*) > 1;`
       If it returns rows, delete or rename the duplicates (these are almost certainly stale ghost rows from old rooms) before proceeding.
    3. Paste and run the full contents of `supabase/migrations/003-pseudo-unique.sql`.
    4. Confirm the index exists: run `SELECT indexname FROM pg_indexes WHERE tablename = 'players' AND indexname = 'idx_players_pseudo_lower';` — it must return one row.
    5. Smoke-test SC-1: open two browser tabs, create a room in tab 1 with pseudo "Nico", and in tab 2 join the same room with pseudo "nico" (different case). The second join must be rejected (after Plan 05 wires the 23505 error message; at the DB level the insert will already error). Tab 2 joining with a different pseudo must still succeed.
    6. Re-run the migration once more to confirm idempotency — it must complete with no error (the IF NOT EXISTS guard skips re-creation).
  </how-to-verify>
  <verify>
    <human-check>The query in step 4 returns one row (index `idx_players_pseudo_lower` exists); the SC-1 smoke test in step 5 rejects the case-insensitive duplicate and accepts a different pseudo; the re-run in step 6 produces no error.</human-check>
  </verify>
  <resume-signal>Type "applied" once the index exists in the DB and the duplicate pre-flight check is clean, or describe any error (especially a duplicate-key failure on CREATE UNIQUE INDEX).</resume-signal>
  <done>The `idx_players_pseudo_lower` index exists in the live database, SC-1 holds in the smoke test, and re-running the migration is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → players INSERT | Untrusted pseudo string crosses into the DB; uniqueness must be enforced server-side, not client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | players INSERT (pseudo uniqueness) | mitigate | DB `UNIQUE INDEX idx_players_pseudo_lower` on `(room_id, LOWER(pseudo))` — cannot be bypassed by any client (direct API call, race between two simultaneous joins); the index is atomic. No client-side pre-check is relied upon (D-01). |
| T-03-02 | Denial of Service | duplicate ghost rows blocking the index creation | accept | Pre-flight duplicate check + manual cleanup in the human checkpoint; existing `cleanup_dead_rooms()` TTL removes stale rooms. Low risk on a small party-game dataset. |
</threat_model>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift verification):
- New file `supabase/migrations/003-pseudo-unique.sql`
- New DB object: unique index `idx_players_pseudo_lower` on `players (room_id, LOWER(pseudo))`
- New idempotent block added to existing `supabase/schema.sql`
- No new TypeScript/React symbols.
</artifacts_this_phase_produces>

<verification>
- Both SQL files contain the `idx_players_pseudo_lower` expression unique index (automated grep via node).
- Human checkpoint confirms the index is live and SC-1 holds (duplicate case-insensitive pseudo rejected, different pseudo accepted).
- Migration is idempotent (re-run produces no error).
</verification>

<success_criteria>
- `idx_players_pseudo_lower` exists in both `003-pseudo-unique.sql` and `schema.sql`, using `CREATE UNIQUE INDEX` (not ADD CONSTRAINT).
- The index is applied to the live database.
- SC-1 verified manually: case-insensitive duplicate pseudo in the same room is rejected.
</success_criteria>

<output>
Create `.planning/phases/03-playtest-quality-fixes/03-03-SUMMARY.md` when done.
</output>
