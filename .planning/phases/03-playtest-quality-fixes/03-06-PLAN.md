---
phase: 03-playtest-quality-fixes
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/room/[code]/game/page.tsx
autonomous: true
requirements: [SC-8, SC-5b, SC-5]
must_haves:
  truths:
    - "During the Type C choice phase the X/N pill shows the frozen round-start count (e.g. 0/3) when a 4th player joins mid-choice, NOT 0/4"
    - "The host 'Passer sans attendre' button on the choice phase only appears while fewer than the frozen-count players have acted"
    - "The Type C choice phase shows NO vote timer; the round advances only when every frozen-count player has acted (or the host force-skips)"
    - "Removing the choice-phase timer does not break resolution — submitChoice still resolves at the frozen count"
  artifacts:
    - path: "app/room/[code]/game/page.tsx"
      provides: "ChoiceScreen with frozen-count display denominator and no VoteTimer"
      contains: "vote_round_player_count"
  key_links:
    - from: "ChoiceScreen VoteProgress (game/page.tsx ~907)"
      to: "gs.vote_round_player_count"
      via: "total={gs.vote_round_player_count || players.length}"
      pattern: "vote_round_player_count \\|\\| players\\.length"
    - from: "ChoiceScreen HostSkipBtn gate (game/page.tsx ~913)"
      to: "gs.vote_round_player_count"
      via: "voteCount < (gs.vote_round_player_count || players.length)"
      pattern: "voteCount < \\(gs\\.vote_round_player_count \\|\\| players\\.length\\)"
---

<objective>
Fix two real local-code gaps in the single file `app/room/[code]/game/page.tsx`, both inside the Type C `ChoiceScreen` (so they are combined into ONE plan to avoid in-file conflicts):

1. **SC-8 (display half-snapshot)** — the round-resolution threshold already uses the frozen `gs.vote_round_player_count` snapshot, but the X/N DISPLAY denominator and the host skip-button gate still read the LIVE `players.length`. A 4th player joining mid-choice makes the pill render 0/4 even though the round still resolves at the frozen 3. Fix the DISPLAY only.

2. **5b (design decision)** — the Type C choice phase must have NO vote timer (locked decision, 2026-06-10). Resolution is timer-independent (`submitChoice` triggers `resolveTypeCChoice` at the frozen count); the host "Passer sans attendre" button is the only AFK fallback. Remove the `VoteTimer` from `ChoiceScreen` and drop the now-unused `isAdvancer` prop.

Optionally (SC-5 robustness) lazily stamp `round_started_at` when a timer-bearing phase is observed with an empty value, anchoring pre-Phase-3 in-flight game rows. SC-5 itself is otherwise **resolved by deployment** (HEAD source is already correct — see note below); no timer-logic code fix is planned.

Purpose: SC-8 (mid-round joiner does not distort the displayed threshold) + the locked 5b design decision (no timer on the Type C choice phase).
Output: edits to `app/room/[code]/game/page.tsx` only; `npx tsc --noEmit` and `next build` stay green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playtest-quality-fixes/03-UAT.md
@.planning/debug/typec-choice-timer-and-threshold.md
@.planning/debug/timer-resets-on-refresh.md
@app/room/[code]/game/page.tsx
</context>

<sc5_deployment_note>
SC-5 (mid-round refresh shows remaining timer) is classified **deployment-only** in the UAT: the refresh-safe `VoteTimer` (`initialSecs = max(0, 30 - elapsed)` derived from `gs.round_started_at`) is already correct in HEAD source at all four call sites; production fails only because the entire Phase-03 commit chain is unpushed and Railway builds from `origin/main`. The resolution is **push + redeploy**, handled by the user after gap execution — NOT a code task. Task 3 below is an OPTIONAL, tiny, isolated robustness enhancement (lazy-stamp) and is the only SC-5-related code touched here. Do NOT modify the four `initialSecs` IIFE blocks for SC-5.
</sc5_deployment_note>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the Type C choice-phase display denominator (SC-8)</name>
  <files>app/room/[code]/game/page.tsx</files>
  <read_first>
    - app/room/[code]/game/page.tsx lines 889–960 (the full `ChoiceScreen` component — its footer at 905–914 renders `VoteProgress` at 907 and `HostSkipBtn` at 913)
    - app/room/[code]/game/page.tsx lines 1719, 1734, 1788–1795, 1903–1917 (where `vote_round_player_count` is SET on choice-phase entry and READ as the resolution threshold — the snapshot is already correct; the display is the only gap)
    - app/room/[code]/game/page.tsx lines 251–266 (`VoteProgress` — confirms `total` is the X/N denominator surfaced to the user)
    - .planning/debug/typec-choice-timer-and-threshold.md (root cause: "display denominator uses live players.length"; the minimal fix is the two lines below)
  </read_first>
  <action>
    In `ChoiceScreen` (app/room/[code]/game/page.tsx), change the DISPLAY/gating to use the frozen snapshot instead of the live roster — exactly two edits, no resolution-logic changes:
    - At the `VoteProgress` call (currently `total={players.length}`, ~line 907) change the `total` prop to `total={gs.vote_round_player_count || players.length}`.
    - At the `HostSkipBtn` gate (currently `show={isHost && hasVoted && voteCount < players.length}`, ~line 913) change the comparison to `show={isHost && hasVoted && voteCount < (gs.vote_round_player_count || players.length)}`.
    KEEP the `|| players.length` fallback in BOTH places: `vote_round_player_count` is `0` for pre-Phase-3 in-flight game rows (the factory default from `makeInitialGameState`), and `0 || players.length` correctly falls back to the live roster for those rows. Do NOT touch any other VoteProgress/HostSkipBtn call site in the file (other vote phases are out of scope for SC-8 — the reported symptom is strictly the Type C choice phase). Do NOT change the `others` designation list at ~line 900.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/room/[code]/game/page.tsx','utf8'); const ok = s.includes('total={gs.vote_round_player_count || players.length}') && /voteCount < \(gs\.vote_round_player_count \|\| players\.length\)/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `ChoiceScreen`'s `VoteProgress` uses `total={gs.vote_round_player_count || players.length}`
    - `ChoiceScreen`'s `HostSkipBtn` gate uses `voteCount < (gs.vote_round_player_count || players.length)`
    - The `|| players.length` fallback is preserved in both edits
    - No other vote phase's display denominator was changed
  </acceptance_criteria>
  <done>A 4th player joining during the Type C choice phase no longer changes the displayed denominator (stays X/3, matching the frozen resolution threshold); the host skip button hides once the frozen-count players have acted.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the vote timer from the Type C choice phase (5b)</name>
  <files>app/room/[code]/game/page.tsx</files>
  <read_first>
    - app/room/[code]/game/page.tsx lines 905–914 (the `ChoiceScreen` footer: `VoteProgress` at 907, the `VoteTimer` IIFE at 908–912, `HostSkipBtn` at 913)
    - app/room/[code]/game/page.tsx lines 891–895 (the `ChoiceScreen` props destructure + type signature — `isAdvancer` is declared here and becomes unused once the timer is gone)
    - app/room/[code]/game/page.tsx line 1941 (the `<ChoiceScreen ... isAdvancer={isAdvancer} ...>` call site — drop the prop here too)
    - app/room/[code]/game/page.tsx lines 1788–1795 (`submitChoice` resolution: `resolveTypeCChoice` fires at `count >= frozen threshold` — timer-independent, confirms removing the timer does not break advancement)
    - .planning/debug/typec-choice-timer-and-threshold.md (Issue 2 fix direction: delete the VoteTimer IIFE, leave VoteProgress + HostSkipBtn, drop isAdvancer; host "Passer sans attendre" remains the sole AFK fallback)
  </read_first>
  <action>
    Per the locked design decision (2026-06-10), the Type C choice phase must have NO vote timer:
    - In `ChoiceScreen`'s footer, DELETE the IIFE block at ~lines 908–912 that computes `elapsed`/`initialSecs` and returns `<VoteTimer key={`vt-${gs.round}`} isAdvancer={isAdvancer} onExpire={onForce} initialSecs={initialSecs} />`. Leave `VoteProgress` (~907) and `HostSkipBtn` (~913) intact. Removing the timer removes the advancer auto-skip; the host's `HostSkipBtn` ("Passer sans attendre") at ~913 → `onForce` → `resolveTypeCChoice` remains the only AFK fallback (consistent with how `HostSkipBtn` works on other phases: `isHost && hasVoted`).
    - Remove the now-unused `isAdvancer` from `ChoiceScreen`: delete it from the destructured params and from the inline prop type at ~lines 891–895, and remove `isAdvancer={isAdvancer}` from the `<ChoiceScreen ...>` call site at ~line 1941. This is cosmetic cleanup — `isAdvancer` is still defined and used by the OTHER screens (`QuestionSelectionScreen`, `DesignationVoteScreen`, `ConfessionVoteScreen`), so do NOT remove its definition or its use elsewhere.
    - Do NOT change `resolveTypeCChoice`, `submitChoice`, or `onForce` wiring — resolution already works at the frozen count and is timer-independent.
    Confirm the host can still force-skip after they have acted: `HostSkipBtn` requires `isHost && hasVoted`, and the host is always a player, so once they volunteer/designate `hasVoted` is true and the button shows while `voteCount < frozen count`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/room/[code]/game/page.tsx','utf8'); const ci=s.indexOf('function ChoiceScreen'); const cj=s.indexOf('function VolunteersRevealScreen'); const body=s.slice(ci,cj); const ok = ci>-1 && cj>ci && !body.includes('VoteTimer') && !body.includes('isAdvancer'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The `ChoiceScreen` component body contains NO `VoteTimer` reference
    - The `ChoiceScreen` component body contains NO `isAdvancer` reference (removed from params, type, and call site)
    - `VoteProgress` and `HostSkipBtn` remain in the `ChoiceScreen` footer
    - `isAdvancer` is still defined and used by the other vote-phase screens (unchanged)
    - `resolveTypeCChoice` / `submitChoice` are unchanged
  </acceptance_criteria>
  <done>The Type C choice phase renders no timer; it advances when every frozen-count player has acted, with the host "Passer sans attendre" button as the sole AFK fallback. The unused `isAdvancer` prop is dropped from `ChoiceScreen` only.</done>
</task>

<task type="auto">
  <name>Task 3 (OPTIONAL robustness): Lazily stamp empty round_started_at for legacy in-flight rows (SC-5 anchor)</name>
  <files>app/room/[code]/game/page.tsx</files>
  <read_first>
    - app/room/[code]/game/page.tsx lines 482–516, 719–721, 909–911 (the four `initialSecs` IIFE call sites — DO NOT change these; they are already correct)
    - app/room/[code]/game/page.tsx — locate `applyRoom` / the effect that sets `gs` from the realtime room payload (the single place where a freshly-loaded `game_state` lands in React state on mount/refresh), and the `advance(...)` / `updateRoomGameState` helper used to persist `game_state`
    - .planning/debug/timer-resets-on-refresh.md (Resolution → SECONDARY: "lazily stamp round_started_at when a timer-bearing phase is observed with an empty value… minor, not required for SC-5")
  </read_first>
  <action>
    KEEP THIS TINY AND ISOLATED. This is the ONLY SC-5 code change; everything else about SC-5 is resolved by the user pushing + redeploying HEAD (see <sc5_deployment_note>). Goal: for a game row created under a pre-Phase-3 build (whose stored `game_state.round_started_at` is `''`/undefined), when the local client first observes a TIMER-BEARING phase with an empty `round_started_at`, write `new Date().toISOString()` once so the timer anchors from the observation moment instead of perpetually resetting to a fresh 30s on every refresh.

    Implementation guidance (choose the least-invasive option that fits the existing structure):
    - Add a small one-shot effect (or a guarded branch inside the existing room-apply effect) that runs only when ALL of: the current `gs.phase` is one of the timer-bearing phases `voting_question | round_a_vote | round_b_vote | round_c_choice`, AND `gs.round_started_at` is falsy (`''`/null/undefined), AND this client is the elected advancer (smallest present `player.id`, the same election already used for `onForce`) to avoid every client writing simultaneously.
    - In that branch, persist `{ ...gs, round_started_at: new Date().toISOString() }` via the existing `advance(...)`/`updateRoomGameState` write path. Guard with a ref so it fires at most once per mount (do not loop on the resulting realtime echo).
    - Do NOT alter the `initialSecs` math, the `VoteTimer` component, `makeInitialGameState` (it must keep initializing `round_started_at: ''`), or any standard write site (`startGame`, `resolveVotes`, `onNextRound`) that already stamps a real timestamp.

    If, after reading the code, this cannot be done in a small, isolated, clearly-safe way (e.g. it would require restructuring the room-apply flow), SKIP it: leave the timer logic untouched and record in the SUMMARY that the lazy-stamp was intentionally skipped and SC-5 is resolved purely by deployment. The plan still succeeds.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Either: a one-shot, advancer-gated lazy-stamp writes `round_started_at` only when a timer-bearing phase is observed with an empty value, touching no other timer logic; OR the task is intentionally skipped and documented in the SUMMARY.
    - `makeInitialGameState` still initializes `round_started_at: ''` (unchanged)
    - The four `initialSecs` IIFE blocks are unchanged
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Legacy pre-Phase-3 in-flight rows anchor their timer from first observation (or the enhancement is cleanly skipped and documented); no regression to the already-correct refresh-safe timer.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors from the dropped `isAdvancer` prop or any edit).
- `next build` completes successfully.
- Automated greps confirm: ChoiceScreen uses the frozen denominator in both `VoteProgress` and `HostSkipBtn`; ChoiceScreen contains no `VoteTimer` and no `isAdvancer`.
- Manual (post-deploy, by user): a 4th player joining mid Type C choice shows X/3 (not X/4); the choice phase shows no countdown timer; the round advances once the 3 frozen players act or the host taps "Passer sans attendre".
</verification>

<success_criteria>
- SC-8: the displayed denominator on the Type C choice phase stays frozen at the round-start count when a player joins mid-choice.
- 5b: the Type C choice phase has no vote timer; resolution is gated on frozen-count actions with the host force-skip as the only AFK fallback.
- SC-5: timer logic untouched (deployment-resolved); optional lazy-stamp added or cleanly skipped.
- `tsc --noEmit` and `next build` are green.
</success_criteria>

<output>
Create `.planning/phases/03-playtest-quality-fixes/03-06-SUMMARY.md` when done. Record whether the optional Task 3 lazy-stamp was implemented or intentionally skipped, and restate that SC-5 is otherwise resolved by the user pushing + redeploying HEAD.
</output>
