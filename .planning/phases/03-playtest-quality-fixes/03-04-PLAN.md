---
phase: 03-playtest-quality-fixes
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - app/room/[code]/game/page.tsx
autonomous: true
requirements: [SC-5, SC-7, SC-8]
must_haves:
  truths:
    - "A player who refreshes mid-round sees the correct remaining timer (not always 30s)"
    - "Vote phases resolve against the round's player-count snapshot, not the live roster"
    - "A player joining mid-round is excluded from the current round's vote threshold"
    - "All players see a brief toast when someone joins mid-round"
    - "Type C with 0 volunteers shows the roulette, never a 'répond à voix haute' message"
  artifacts:
    - path: "app/room/[code]/game/page.tsx"
      provides: "VoteTimer initialSecs prop, snapshot threshold, player_joined toast+broadcast, Type C 0-volunteer guard"
      contains: "initialSecs"
  key_links:
    - from: "VoteTimer call sites"
      to: "gs.round_started_at"
      via: "initialSecs = max(0, 30 - elapsed)"
      pattern: "round_started_at"
    - from: "submitVote / submitChoice / resolveOnShrinkRef"
      to: "gs.vote_round_player_count"
      via: "threshold = gs.vote_round_player_count || players.length"
      pattern: "vote_round_player_count"
    - from: "players INSERT handler"
      to: "votes-broadcast channel"
      via: "send player_joined broadcast"
      pattern: "player_joined"
---

<objective>
Thread the two new GameState fields through the game page and add the mid-round-join toast and the Type C 0-volunteer guard. Five surgical change clusters in one file (`app/room/[code]/game/page.tsx`): (a) `VoteTimer` accepts `initialSecs` and every voting phase sets `round_started_at`/`vote_round_player_count`; (b) vote-resolution thresholds use the snapshot with a `|| players.length` fallback; (c) the players-INSERT handler broadcasts `player_joined` and a toast renders it; (d) the Type C reveal screen is hardened so 0 volunteers never shows the "répond à voix haute" text.

Purpose: SC-5 (refresh-safe timer), SC-8 (mid-round joiner excluded from threshold + join notification), SC-7 (Type C 0-volunteer → roulette).
Output: Updated `app/room/[code]/game/page.tsx`.

NOTE: This plan owns `app/room/[code]/game/page.tsx` exclusively. It runs in Wave 2 after Plan 01 (GameState fields) and Plan 02 (the `game.player_joined` i18n key) land.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playtest-quality-fixes/03-CONTEXT.md
@.planning/phases/03-playtest-quality-fixes/03-RESEARCH.md
@.planning/phases/03-playtest-quality-fixes/03-PATTERNS.md
@.planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md
@lib/types.ts
@lib/i18n.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh-safe VoteTimer + snapshot-based vote threshold (D-07, D-09)</name>
  <files>app/room/[code]/game/page.tsx</files>
  <read_first>
    - app/room/[code]/game/page.tsx (VoteTimer definition lines 301–350; the SVG arc uses `secs / 30`; VoteTimer call sites at lines 481, 509, 710, 896; the resolution threshold checks `if (count >= players.length)` at lines 1664 and 1733; the `resolveVotes` question_selection transition at lines 1675–1679; `onNextRound` transition at lines 1789–1795; `resolveOnShrinkRef.current` at lines 1840–1854; `playersRef` updated synchronously in INSERT/DELETE handlers around lines 1563/1572/1583)
    - lib/types.ts (the new `round_started_at` / `vote_round_player_count` fields from Plan 01)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (section "app/room/[code]/game/page.tsx" change-sites 1–4)
    - .planning/phases/03-playtest-quality-fixes/03-RESEARCH.md (Patterns 3 & 4; Pitfalls 2, 3, 4, 7 — fallbacks and the playersRef-vs-state snapshot rule)
  </read_first>
  <action>
    Make four edits in `app/room/[code]/game/page.tsx`:
    (1) VoteTimer signature: add an optional `initialSecs = 30` prop to `function VoteTimer({ isAdvancer, onExpire })` and initialise `const [secs, setSecs] = useState(initialSecs)`. Leave the SVG arc denominator as `secs / 30` (the full duration, NOT initialSecs) and leave the existing `fired` ref logic unchanged.
    (2) At EACH of the four VoteTimer call sites (lines 481, 509, 710, 896), compute, just before the JSX in that screen's render body: `const elapsed = gs.round_started_at ? Math.floor((Date.now() - new Date(gs.round_started_at).getTime()) / 1000) : 0` then `const initialSecs = Math.max(0, 30 - elapsed)`, and pass `initialSecs={initialSecs}` to `<VoteTimer key={\`vt-${gs.round}\`} ... />`. The `gs.round_started_at ? ... : 0` guard is required so in-flight games with no timestamp (undefined → `new Date(undefined)` is NaN) safely start at 30s (Pitfall 3).
    (3) Vote threshold: at line 1664 replace `if (count >= players.length) await resolveVotes(voteType)` with `const threshold = gs!.vote_round_player_count || players.length; if (count >= threshold) await resolveVotes(voteType)`. At line 1733 replace `if (count >= players.length) await resolveTypeCChoice()` with the same `threshold` computation then `if (count >= threshold) await resolveTypeCChoice()`. In `resolveOnShrinkRef.current` (around line 1849) replace `if (count >= players.length)` with `if (count >= (gs.vote_round_player_count || players.length))`. The `|| players.length` fallback is mandatory: in-flight games have `vote_round_player_count === undefined`, and `count >= undefined` is always false → the phase would hang forever (Pitfall 2). Do NOT change the `VoteProgress total={players.length}` denominator anywhere — players see the live roster, only the resolution threshold uses the snapshot (RESEARCH anti-pattern).
    (4) Snapshot the new fields at every voting-phase entry. In `resolveVotes` question_selection transition (the `advance({...})` at lines 1675–1679) add `round_started_at: new Date().toISOString(), vote_round_player_count: playersRef.current.length,`. In `onNextRound` (the `advance({...})` at lines 1789–1795) add the same two fields. Use `playersRef.current.length`, NOT `players.length`, to avoid a stale-closure off-by-one when a player joins right as the round starts (Pitfall 4).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `VoteTimer` declares an `initialSecs` prop and `useState(initialSecs)`
    - All four `<VoteTimer ... />` usages pass an `initialSecs={...}` prop
    - The file contains the guard `gs.round_started_at ? Math.floor(` (refresh-safe elapsed calc)
    - The file contains `gs!.vote_round_player_count || players.length` (or equivalent `gs.vote_round_player_count || players.length`) at the two resolution sites and in `resolveOnShrinkRef`
    - The two `advance({...})` voting-phase transitions include `round_started_at: new Date().toISOString()` and `vote_round_player_count: playersRef.current.length`
    - `VoteProgress` still receives `total={players.length}` (unchanged)
    - `npm run build` passes
  </acceptance_criteria>
  <done>VoteTimer initialises from `round_started_at` (refresh-safe, clamped to [0,30]); resolution thresholds use the snapshot with a `|| players.length` fallback; both voting transitions snapshot the new fields from `playersRef.current.length`.</done>
</task>

<task type="auto">
  <name>Task 2: Mid-round join toast + player_joined broadcast (D-10)</name>
  <files>app/room/[code]/game/page.tsx</files>
  <read_first>
    - app/room/[code]/game/page.tsx (the players-INSERT `postgres_changes` handler at lines 1556–1566 that sets `playersRef.current`; the `voteChannelRef` broadcast `.on(...)` chain and `.send(...)` emit pattern around lines 1545–1662; `roomRef.current?.game_state`; the main `return (...)` wrapping `GameControlsCtx.Provider` at lines 1893–1897; the `C` color constant)
    - lib/i18n.ts (the `game.player_joined(pseudo)` function key from Plan 02)
    - .planning/phases/03-playtest-quality-fixes/03-PATTERNS.md (change-sites 5 & 6 — toast state, broadcast listener, emit-from-INSERT, JSX)
    - .planning/phases/03-playtest-quality-fixes/03-UI-SPEC.md ("Mid-Round Join Toast" spec — fixed top-center pill, 2.5s, role=status, max 280px)
    - .planning/phases/03-playtest-quality-fixes/03-RESEARCH.md (Pattern 6 & 7; Pitfalls 4 & 5 — toast only on game page, clear previous timer with a ref)
  </read_first>
  <action>
    Three edits in `app/room/[code]/game/page.tsx`:
    (1) Add toast state near the other component state: `const [toastMessage, setToastMessage] = useState<string | null>(null)` and `const toastTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)`.
    (2) Emit on join: in the players-INSERT `postgres_changes` handler (after the `setPlayers(...)` / `playersRef.current = next` update, ~line 1563), add a broadcast send guarded by an active non-ended game: read `const currentGs = roomRef.current?.game_state` and `if (currentGs && currentGs.phase !== 'ended') { voteChannelRef.current?.send({ type: 'broadcast', event: 'player_joined', payload: { pseudo: (payload.new as Player).pseudo } }) }`. Guard against re-broadcasting for a player already in `prev` (the existing `if (prev.find((p) => p.id === payload.new.id)) return prev` already dedupes the roster; only emit when a genuinely new row is added).
    (3) Receive + render: add a `.on('broadcast', { event: 'player_joined' }, ({ payload }) => { if (toastTimerRef.current) clearTimeout(toastTimerRef.current); setToastMessage(fr.game.player_joined(payload.pseudo as string)); toastTimerRef.current = setTimeout(() => setToastMessage(null), 2500) })` to the same `voteChannel` `.on('broadcast', ...)` chain where `vote_count`/`phase_changed` are handled. Clearing the previous timer before setting a new one prevents piled-up timers on rapid successive joins (Pitfall: toast without a ref). Then render the toast in the returned JSX (inside the `<GameControlsCtx.Provider>` return, alongside `{screen}`): a conditional `{toastMessage && (<div role="status" aria-live="polite" style={{ position:'fixed', top:16, left:'50%', transform:'translateX(-50%)', zIndex:50, background:C.surface, border:\`1px solid ${C.border}\`, borderRadius:24, padding:'8px 16px', maxWidth:280, color:'#fff', fontSize:14, fontWeight:500, fontFamily:'var(--font-body)' }}>{toastMessage}</div>)}`. The toast lives only on the game page (the lobby and end screen never subscribe to this channel — Pitfall 5), so no cross-page guard is needed beyond the `phase !== 'ended'` emit guard.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The file declares `toastMessage` state and a `toastTimerRef`
    - The players-INSERT handler sends a broadcast with `event: 'player_joined'` and `payload: { pseudo: ... }`, guarded by `currentGs.phase !== 'ended'`
    - A `.on('broadcast', { event: 'player_joined' }, ...)` listener calls `fr.game.player_joined(...)` and `setTimeout(..., 2500)`, clearing `toastTimerRef.current` first
    - The JSX renders a conditional toast `<div role="status" aria-live="polite" ...>` positioned `fixed` at `top:16` / `left:'50%'` with `maxWidth:280`
    - `npm run build` passes
  </acceptance_criteria>
  <done>Joining mid-round broadcasts `player_joined`; all clients show a 2.5s top-center toast using `fr.game.player_joined`; the toast never appears after the game ends.</done>
</task>

<task type="auto">
  <name>Task 3: Type C 0-volunteer hardening — never show "répond à voix haute" (D-11)</name>
  <files>app/room/[code]/game/page.tsx</files>
  <read_first>
    - app/room/[code]/game/page.tsx (`resolveTypeCChoice` at lines 1736–1755 — already guards `if (vols.length > 0)` before advancing to `round_c_volunteers_reveal`, else advances to `round_c_roulette`; the `VolunteersRevealScreen` component at lines 948–996 which renders `vols.length === 1 ? fr.question_ouverte.volunteers_reveal_one(vols[0].pseudo) : fr.question_ouverte.volunteers_reveal_many` at line 989; `vols` is derived from `gs.volunteer_player_ids` at lines 956–958)
    - .planning/phases/03-playtest-quality-fixes/03-CONTEXT.md (D-11 — the message must only appear when `volunteer_player_ids.length >= 1`)
    - .planning/phases/03-playtest-quality-fixes/03-RESEARCH.md (Assumption A1 + Open Question 1 — the transition guard is correct; the defensive bug is `vols[0].pseudo` crashing when vols is empty)
  </read_first>
  <action>
    The transition logic in `resolveTypeCChoice` (lines 1736–1755) is already correct: when `vols.length === 0` it advances to `round_c_roulette`, never to `round_c_volunteers_reveal`. Leave that logic intact. Add a DEFENSIVE guard in `VolunteersRevealScreen` so a 0-volunteer state can never render the "répond à voix haute" text or crash on `vols[0].pseudo`: at the top of the component's render (after `const vols = ...` at lines 956–958), if `vols.length === 0`, render the same `GameScreen` shell with the question card and the host "next round" footer but WITHOUT the volunteers title/avatars/`volunteers_reveal_*` text — i.e. early-return a minimal "no one volunteered" view (reuse an existing neutral i18n string if one fits, otherwise show only the question + footer with no reveal sentence). Critically, the existing line 989 (`vols.length === 1 ? ...volunteers_reveal_one(vols[0].pseudo) : ...volunteers_reveal_many`) must only execute when `vols.length >= 1`, so `vols[0]` is never `undefined`. Do NOT change `resolveTypeCChoice`'s `tallyDesignation`/roulette path. This is purely a render-side safety net per D-11 and RESEARCH Assumption A1.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `resolveTypeCChoice` still advances to `round_c_roulette` when `vols.length === 0` and to `round_c_volunteers_reveal` only when `vols.length > 0` (unchanged transition)
    - `VolunteersRevealScreen` early-returns or otherwise skips the `volunteers_reveal_one`/`volunteers_reveal_many` text when `vols.length === 0`
    - The expression `vols[0].pseudo` (or `vols[0]?.pseudo`) is never evaluated with an empty `vols` array
    - `npm run build` passes
  </acceptance_criteria>
  <done>Type C with 0 volunteers transitions to the roulette and, even if `round_c_volunteers_reveal` is reached with an empty list, the screen never renders a "répond à voix haute" sentence and never dereferences `vols[0]`.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift verification):
- `VoteTimer` gains an `initialSecs?: number` prop (existing component, new prop) in `app/room/[code]/game/page.tsx`
- `toastMessage` state + `toastTimerRef` ref (local to the game page component)
- New broadcast event name `player_joined` on the `votes-broadcast-${id}` channel (payload `{ pseudo: string }`)
- No new exported functions, files, or components. `resolveTypeCChoice` and `VolunteersRevealScreen` are existing symbols with hardened logic.
</artifacts_this_phase_produces>

<verification>
- `npm run build` passes after all three tasks.
- Manual (per 03-VALIDATION.md): refresh mid-round shows correct remaining time (SC-5); 3rd player joining mid-vote does not block resolution and triggers a toast (SC-8); Type C with everyone sending someone else shows the roulette, no "répond à voix haute" (SC-7).
</verification>

<success_criteria>
- Refresh-safe VoteTimer driven by `round_started_at`, clamped [0,30] (SC-5).
- Vote resolution uses `vote_round_player_count || players.length`; mid-round joiners excluded from current threshold; join toast shown (SC-8).
- Type C 0-volunteer path shows roulette and never the "répond à voix haute" text (SC-7).
- Build green.
</success_criteria>

<output>
Create `.planning/phases/03-playtest-quality-fixes/03-04-SUMMARY.md` when done.
</output>
